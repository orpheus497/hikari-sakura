# Project Progress tracking

*Last Updated:* 2026-08-22 21:35

| Phase | Description | Status |
|---|---|---|
| **Phase 89** | `zwlr_foreign_toplevel_management_v1` — the acting half | **IMPLEMENTED, COMPILES CLEAN, NOT YET RUN.** Phase 88 gave docks the ability to *see* windows; this gives them the ability to *act* on one — focus, close, minimise, maximise. It is the only route: no standards-track toplevel-control protocol exists, and `xdg-activation-v1` cannot substitute because its `activate` takes a `wl_surface` the requester owns, which a switcher does not have for another client's window. New `src/foreign_toplevel.c` (523 lines) per AGENTS.md separation rather than growing `view.c`; one pointer in `hikari_view`; lifecycle mirroring the ext-list handle exactly — `init` in `hikari_view_init`, `create` on map, `destroy` on unmap **and** in `fini`, because BLUEPRINT §15 records three init-failure paths that call `fini` on a view that never mapped. **The modal hazard was the point of the exercise:** `hikari_workspace_focus_view()` opens with `assert(hikari_server_in_normal_mode())`, and a foreign-toplevel request is client-driven — it can arrive mid-drag in move mode, in mark-select, or on a locked screen. Every handler gates on a single `can_act()` (`hikari_server_in_normal_mode() && is_mapped && !is_forced`); because lock mode is not normal mode, one test closes both the modal-abort hole and the act-on-a-locked-session hole. **Activation deliberately does not call `hikari_workspace_focus_view()`** — workspaces are per-output and output focus follows the cursor, so focusing a view on another output through that function would leave `hikari_server.workspace` naming the old one; it uses the proven mark sequence instead (switch sheet if needed → show-or-raise → `center_cursor` → `cursor_focus`). Double-request safety is real, not assumed: `hikari_view_hide()` asserts `!hidden` and `show()` asserts `hidden`, so both minimise directions return early when already in the requested state, and the activate path **re-tests** `is_hidden` after a sheet switch because switching shows the incoming sheet's views. Fullscreen maps to full-maximize, matching the precedent already set for client-side xdg fullscreen at `xdg_view.c:656` — so `set_fullscreen` and `set_maximized` are the same operation and a client sees back a state it did not ask for; **an external switcher should expose maximise only.** `app_id` comes from `view->id` (the Phase 88 scoping correction applies again — no new string storage). Gated behind `WITH_FOREIGN_TOPLEVEL_MANAGEMENT`, default YES, because the wlroots header calls itself unstable and its listing half is already superseded by ext-list. **Compiles with 0 warnings at project flags.** A full `make` is currently blocked by root-owned `.o` files from a `sudo make` at 16:18 — `sudo make clean` clears it. Not yet run, so nothing here is hardware-confirmed. |
| **Phase 88** | R2 — `ext-foreign-toplevel-list-v1`; Side-Panel Intent Documented | **DONE — CONFIRMED ON HARDWARE (waybar lists windows, 2026-08-22).** The user asked for taskbar support toward a stated goal — *a side panel that slides in and out from the left, listing applications*. This protocol is that goal's enabling dependency: without it nothing outside the compositor can discover open windows, so a panel would have nothing to list. **One handle per mapped view**, created on map and destroyed on unmap — so a closed window leaves a dock rather than lingering as an entry nothing can act on — with title and app_id republished live from both `set_title` and `set_app_id`. **A scoping correction worth keeping:** the Phase 84 plan asserted views do not retain `app_id`; they do, as `view->id`, set from `hikari_view_configure()` and freed in `fini`. So R2 was one pointer, not a pointer and a string — materially smaller than planned. The error has the same shape as Phase 84's omission of R10: **planning from what was expected rather than from what is there.** Ordering was verified rather than assumed, using BLUEPRINT §15 (written in Phase 86 for exactly this) — `configure` precedes `map`, so the first published state already carries the app_id, and `fini` must also release the handle because §15 records three init-failure paths that call it on a view that never mapped. 0 warnings across all three build configurations; all four wlroots symbols confirmed exported. **The side panel itself is documented as future intent only** (`PLANS.md` item -15), unscoped and unapproved — with the note that it can be an ordinary **layer-shell client** rather than compositor code, and that lock mode already disables the whole `top` tree so it cannot leak window titles onto a locked screen. **Confirmed with waybar** — an external dock now enumerates hikari's windows, which nothing outside the compositor could do before. This is also the last of the original W0-W8 workstreams: **W7b closes, and no original workstream remains open.** |
| **Phase 87** | R3 Deferred Indefinitely; R8 Resolved — Both User Decisions | Documentation only, no code. **R3** (`forced` flag removal) deferred at the user's decision, matching the Phase 84 recommendation: highest risk in the programme, **no user-visible benefit**. Recorded as *closed rather than postponed* — it is not a latent defect, and BLUEPRINT §15 already records which branches the Phase 73 invariant makes unreachable, so the analysis need not be repeated. **R8** resolved by correcting me: I had recommended changing `.clang-format` to match the tree; the user stated the **config is the desired house style**, so the *tree* is what should eventually move. Reformatting deferred for now — a whole-tree reformat would bury real changes in diff noise. |
| **Phase 86** | R6 + R10-a — Buffer Shim Retired; View Ownership Graph Written | **R6:** the `hikari_server_create_argb8888_buffer` shim W1 kept "for one release" is retired — three callers moved to `hikari_buffer_create_argb8888()`, shim and declaration deleted, no references remaining. Taken deliberately *early* rather than left to age: a shim kept "for one release" is exactly the kind of entry that becomes permanent by repetition, which is this session's recurring failure mode. **R10-a:** BLUEPRINT §15 (View Ownership Graph) written — what owns a view, what each lifecycle hook may assume, and specifically the **three init-failure paths that call `hikari_view_fini()` on a view that never mapped**. Written as reference for future `view.c` work; it paid for itself immediately in Phase 88. |
| **Phase 85** | R1 Executed — Tracker Stale-Sweep | Documentation only, no code. `TODOS.md` went from **85 unchecked items to 16**: 55 verified stale, 22 consolidated, 3 counts corrected. This was sequenced **first** in the R-programme deliberately, because every later priority reasons from that list, and Phase 83 had just shown what happens when it doesn't — FB-4 was carried as an open CRITICAL blocker for ~60 phases after becoming false, and phases *reasoned from* it. The sweep is the file-scale version of the same disease. |
| **Phase 84** | Remaining-Work Programme R1–R9 Planned — No Code Changed | Planning only, awaiting approval. Everything open after Phases 70–83 scoped into nine items with dependencies, estimates, acceptance criteria and risk notes (`PLANS.md` item -14). **Sequencing principles encoded from this session's own failures:** one risky change per build cycle; `src/view.c` always gets its own cycle; re-verify recorded environmental claims before acting on them; use a library's constructors rather than hand-building its structs. **Two honest scoping judgements:** R3 flagged deferrable indefinitely because it delivers no user-visible benefit at the highest risk, and R4 **gated on two minutes of user testing** rather than implemented speculatively. **The plan itself then demonstrated the failure it warns about** — it omitted an entire workstream (later added as R10), having been written from what was expected rather than from what is there. |
| **Phase 83** | The eDP-1 Blocker Was Stale — Closed After ~60 Phases as CRITICAL | Documentation correction, no code. Asked directly, the user confirmed the laptop's built-in panel "has been working for a long time". **FB-4 — the eDP-1 scanout swapchain failure — has not been a live problem for a long time**, despite being carried as an open CRITICAL blocker since Phase 19 and referenced in all seven trackers. It was genuinely real when recorded (observed live in Phase 19 with a corroborating `EGL_EXT_device_drm` failure) and was fixed somewhere below hikari — Mesa is now 26.1.6, libdrm 2.4.134 — with **no code change in this project ever needed**. **Why it survived is the part worth keeping:** nothing re-verified it, and phases *reasoned from* it — Phase 70 built the H0 hybrid-graphics hypothesis on top of it, Phase 72 shaped `hikari_platform_probe()` partly to diagnose it, and Phase 81 called it "the single highest-value command available". The documentation treated **recorded** as **still true**, and the entry gained weight by repetition rather than evidence; a blocker attributed to "the layer below hikari" reads as someone else's problem and therefore as permanent. Process consequence recorded: long-lived environmental entries need a *last-confirmed date* and re-verification before being cited as a reason to act. **Applied:** FB-4 closed; **FB-3 downgraded to present-but-harmless** (only ever tracked as FB-4's prime suspect — and pinning a DRM device pre-emptively is explicitly rejected, as it would hard-code a choice the stack is making correctly and become a stale workaround itself); §5 marked HISTORICAL with a note not to run its H1/H2/H3 matrix; **§13 now lists no known-open defect**; the **W0 matrix is largely moot** with only W0-6 (~2 min, gates F4/P2-14) still worth running; and the **leading OBS hypothesis is substantially weakened** — cross-GPU dmabuf presumed wlroots renders and scans out on different devices, which eDP-1 working argues against, so it is downgraded rather than deleted and nothing should be built on it. |
| **Phase 82** | Man Page Documents the Lock Screen; libdrm Declined; Clock Offset Left Fixed | Implemented (documentation). `share/man/man1/hikari.md` now documents all nine `ui { lock { … } }` keys, matching `hikari.conf`; verified both ways and `pandoc --to man` converts cleanly. **Two existing entries were factually wrong and were corrected rather than supplemented:** the **`lock` action** still read "turn off all outputs" and pointed at `public` views as the way to get a clock — both false since Phase 77 — and now describes the blurred backdrop, the compositor-drawn clock, deferred/configurable blanking, and the Phase 73 security property that **every non-public view is hidden because the whole desktop layer is switched off**; it also drops the stale claim that the unlocker must be "in the **PATH**", since Phase 38 made it a compile-time absolute path. **`view-toggle-public`** no longer cites clocks as the reason to mark a view public — that was upstream's workaround for having no compositor-drawn clock — and now points at the `lock` subsection; the mechanism itself is untouched (Total Feature Retention). **libdrm declined, and the honest finding is that it was never a necessity:** `hikari_platform_probe()` already resolves the renderer's DRM node to a path with no added dependency, and libdrm would only have printed the driver name instead, saving one manual lookup performed once — proposing it as "strictly better FB-3 evidence" overstated the gain against a real link dependency the user would only accept vendored. **Clock offset left fixed** at the user's confirmation; it is already a real centimetre derived from EDID, so it holds across display densities without tuning. |
| **Phase 81** | portal-wlr Adopted as the Supported Screen-Sharing Path | Documentation only, no code. At the user's direction — *"i feel like we should stick to portal-wlr"* — xdg-desktop-portal-wlr is recorded as **the** supported backend; alternatives are not to be pursued. **All four compositor-side links are verified working:** `grim` captures 3840×1200 at 95% non-black, `XDG_CURRENT_DESKTOP` resolves the portal correctly, `WAYLAND_DISPLAY` reaches the D-Bus activation environment, and PipeWire is running. OBS ScreenCast remains black and is recorded as **downstream and open — not asserted as an OBS bug**, because the evidence does not reach that far. BLUEPRINT §14 written to hold the whole chain in one place. |
| **Phase 80** | ext-image-copy-capture Made Opt-In — Phase 78 Caused the Black Capture | Implemented — root cause **proven, and it was self-inflicted**. Three facts: `grim` captures correctly against the live session (3840×1200, 1520/1600 samples non-black), so `wlr-screencopy` works; portal-wlr's own TRACE log says **`wayland: using ext_image_copy_capture`**; and hikari advertises that protocol only because Phase 78 added it. **Phase 78 therefore moved portal-wlr off a working path onto a black one** — `grim` was unaffected because it binds screencopy directly and never faces the choice. Not a regression from a working state (screen sharing was broken before Phase 78 too, for the `XDG_CURRENT_DESKTOP` reason fixed in that same phase), but a foreseeable self-inflicted obstacle: **advertising a protocol changes client behaviour, and a newer protocol is not automatically better on a given machine.** Now behind `WITH_EXT_IMAGE_CAPTURE`, default **off** and deliberately excluded from `WITH_ALL`. Rejected: deleting it (wlroots will eventually force it; AGENTS.md §3), a runtime config key (this is a hardware/driver escape hatch, not a preference, and the four existing protocol toggles are all build flags), and fixing it in hikari (impossible — hikari creates two globals, wlroots implements everything behind them). `force_mod_linear=1` was applied per the DEBUG dump and did **not** help. **Correction recorded:** `dmabuf_device` is an internal variable inside portal-wlr, not a config key — read from a `strings` dump and presented as an interface without checking, the third such error this session. Validated across **three** configs (default / full / full+ext) at 0 warnings; `make -V` confirms off by default, on with the flag, not pulled in by `WITH_ALL`. Documented in `README.md`. **Confirmed installed and running (binary 14:43, ext compiled out).** |
| **Phase 79** | OBS Screen Sharing Diagnosed — `WAYLAND_DISPLAY` Never Reaches the D-Bus Activation Environment | Implemented — user reported W7a/W8 working but screen sharing still failing, guessing an OBS issue. It was not. **`start-hikari.sh` wraps the compositor in `dbus-run-session`, so the session bus starts BEFORE the compositor creates its Wayland socket** — and D-Bus hands every service it activates the environment the bus itself started with, which therefore can never contain `WAYLAND_DISPLAY`. `xdg-desktop-portal-wlr` activated with no idea which compositor to connect to, failed, and the portal reported no ScreenCast provider — **with nothing logging why**, which is exactly why it presented as a client problem. Verified absent in `dbus-run-session`, the session `dbus-daemon` and the running portal. Fixed with `export_activation_environment()`, run after `wlr_backend_start()` so `DISPLAY` is published too, guarded by `command -v` and routed through the detached autostart helper so a missing dbus package costs only this feature. **Confirmed working: portal-wlr now runs and connects.** A second blocker was identified as **not** a code defect — PipeWire/WirePlumber installed but not running; deliberately not hardcoded, since starting a media daemon is session policy and hikari already provides `~/.config/hikari/autostart`. **Diagnostic error recorded:** an initial reading appeared to show `XDG_CURRENT_DESKTOP=Hikari` and cost ~10 minutes of false hypotheses — cause was `procstat -e` printing a space-separated environment and `tr ' ' '\n'` splitting `"Hikari Sakura:wlroots"` at its space. **A value containing a space broke the parser, not the system.** |
| **Phase 78** | W7a + W8 — Modern Capture, the Portal Fix, and XWayland Finally Renders | Implemented, unbuilt — 0 warnings across 64 files in both configs. **W7a:** both capture generations now advertised — `ext-image-capture-source` + `ext-image-copy-capture` beside `wlr-screencopy`, because the wlroots header states screencopy "will be dropped in a future wlroots version" (only-old loses capture on upgrade; only-new breaks every tool that exists today). **Portal fix verified empirically, not assumed:** the installed `wlr.portal` has `UseIn=wlroots;sway;Wayfire;river;phosh;Hyprland;` and `XDG_CURRENT_DESKTOP="Hikari Sakura"` matched **none** of them, so screen sharing had no backend at all *regardless* of which protocols the compositor advertised; now `"Hikari Sakura:wlroots"` plus matching `DesktopNames`. **W8 — XWayland renders content for the first time in this tree.** `xwayland_view.c` attached only hikari's own border and indicator rects, so managed X11 windows drew as empty bordered rectangles; `xwayland_unmanaged_view.c` had **no `wlr_scene` reference whatsoever**, so menus, tooltips, dropdowns and drag icons were hit-tested but never drawn. Both now use `wlr_scene_subsurface_tree_create()` — chosen over `wlr_scene_surface_create()`, whose own docs say "child sub-surfaces are ignored", which would have been a fresh instance of the same bug. Created on **`associate`** (the surface is NULL before it, and valid for the whole associate/dissociate window, which is exactly the tree's proper lifetime). Managed views parent under their per-view `scene_tree`; unmanaged views have no such parent so they attach to `layers.views` in layout-absolute coordinates, raise to top on map (a menu is only meaningful above what spawned it, and the raise is layer-scoped so it cannot climb over the bar or a locked screen), and reposition on commit so pointer-tracking menus follow. **Shared ownership handled without betting on a contract:** wlroots tears these trees down with the surface while hikari destroys them on dissociate, so both register a listener on `wlr_scene_node.events.destroy` that nulls the pointer — neither a double-destroy nor a stale pointer is reachable whichever side goes first. **This is the direct application of the Phase 76 lesson.** Audits performed rather than assumed: all 19 listeners across both files verified removed exactly once, and destroy *ordering* verified so the parent-tree destroy fires the handler before the explicit link removal. **W7b (`ext-foreign-toplevel-list-v1`) deliberately sequenced to the next cycle** — checked and *not* required for screen sharing (portal-wlr captures outputs and has no window picker); it serves taskbars and future window-selection, needs six touch points in `src/view.c` (the file behind eight crash phases), and bundling it would make an X11 crash ambiguous between two independently-risky changes. Sequencing, not a scope cut. |
| **Phase 77** | Lock Screen CONFIRMED WORKING; Clock Raised 1 cm | **DELIVERED** — user rebuilt after Phase 76 and confirmed it works. The native blurred lock screen with a compositor-drawn clock is live: workspace captured and blurred at lock time, clock and date drawn by the compositor needing no client, indicator and public views layered above, power-aware blanking. **Closes W3, W4 and the original Phase 70 request.** Clock raised by a **real centimetre** via `mm_to_logical_pixels()`, deriving the offset from EDID `phys_height` against the mode's pixel height and dividing by output scale (scene nodes use logical coordinates); a pixel constant would be a different physical distance on every panel. Falls back to the 96 DPI convention (10 mm = 37 logical px) where EDID reports no size. The 40 px indicator clearance is untouched, so this is literally 1 cm higher than before rather than a re-derived layout. **Not done, offered instead:** a configurable `clock-offset` — unrequested and AGENTS.md-gated, but worth having since each visual tweak costs a rebuild. **The three-cycle crash sequence, as a unit:** Phase 74 shipped with two bugs caught before release and two that escaped; Phase 75 root-caused crash 1 from a core dump in minutes but shipped a *speculative* second change that was backwards; Phase 76 root-caused crash 2 to the same hand-built struct, fixed it by using the API constructor, and reverted Phase 75's speculation. Lessons recorded: a fallback ladder is only as safe as its first rung (an assert is not recoverable); do not hand-build a library's structs when it ships a constructor, especially with fields documented "do not use"; and **do not ship speculation alongside an evidence-backed fix** — the fix lends the guess unearned credibility, which is exactly why Phase 75's error survived a full cycle. |
| **Phase 76** | Second Crash — Stop Hand-Building `wlr_drm_format` | Implemented — `hikari.4102.1001.core` named **`render/drm_format_set.c:144: assert(src->len <= src->capacity)`**; Phase 75 had set `.len = 1` and left `.capacity = 0`. Two crashes, two invariants of the same hand-constructed struct whose header documents `capacity` as "do not use" — patching one field per crash was treating symptoms, the defect was building it at all. Now constructed via `wlr_drm_format_set_add()`/`_get()`/`_finish()`; no hand-built format or `.capacity` assignment remains in `src/`. **Also reverted Phase 75's other change** (`transformed_resolution` → `width/height`) as backwards: the library's own assert `buffer->width == resolution_width && ...` uses wlroots' naming for the transformed resolution, so rotation is baked into the rendered buffer. **Method note:** enumerating asserts up front failed (stripped library, `objdump` cannot read this FreeBSD ELF), but **`strings` over the .so lists assertion expressions** without symbols — that is how the `resolution_width` assert was found. |
| **Phase 75** | First Crash — Empty Modifier List Aborts the GBM Allocator | Implemented — first post-build crash root-caused from `hikari.26797.1001.core` to **`render/allocator/gbm.c:66: assert(format->len > 0)`**, the assertion strings recovered by disassembling the call site since registers were clobbered by `raise`/`abort`. Phase 74's ladder used `len = 0, modifiers = NULL` intending "implicit modifier", which is invalid — so it aborted on the first lock attempt before reaching the well-formed LINEAR rungs. Correct spelling is a one-entry list holding `DRM_FORMAT_MOD_INVALID`. **Design lesson:** an escalation ladder recovers only from *returned* failures, never assertions — a fallback chain is only as safe as its first rung, and this one read as defensive code while containing a guaranteed abort. **Superseded in part by Phase 76**, which found the deeper cause and reverted this phase's speculative second change. |
| **Phase 74** | W3 + W4 — The Native Blurred Lock Screen with a Clock | Implemented, unbuilt — 6 new files, 8 modified. **User confirmed Phase 73 works on hardware**, then corrected the requirement: the clock is a *native compositor function*, not a client marked `public`. That retires the Phase 70 scoping, which had been too deferential to upstream's workaround; `public` still works (Total Feature Retention) but is no longer the answer. **W3 capture:** renders the output off-screen through `wlr_scene_output_build_state()`'s `swapchain` option — wlroots has no compositor-facing screenshot call, screencopy serves clients — then reads back via `wlr_texture_read_pixels()`, which is glReadPixels-backed and so never needs a CPU-mappable buffer (the FB-2 constraint approached from the other side). **The format spike is resolved** as a logged 4-rung escalation ladder, since 0.20.2 exposes texture-format queries but nothing for render *targets*. **A failure mode caught by reasoning, not testing:** `build_state()` tracks damage and an idle desktop has none — the capture would have worked only while something was animating, i.e. presented as an intermittent bug; fixed with `wlr_output_update_needs_frame()`. **Alpha forced opaque after readback** — XRGB captures carry no alpha so the byte was driver-dependent, and a 0 would have made the backdrop invisible; this also makes the blur arithmetically correct, since premultiplied and straight coincide at full alpha. **W3 blur:** 3-pass separable box blur with a running sum (radius-independent cost), edges **clamped** rather than wrapped (no bleed) or zero-padded (no vignette). **A heap overflow in this phase's own work was caught on re-read:** `box_blur_line()` shared one stride between source and destination — fine horizontally, but the vertical pass walks a *column* while writing a *compact* scratch line, so it would have overrun the heap by roughly the image height; split into two strides. **W4:** the capture runs **before** `override_visibility()` disables the desktop layers (capture after, and it photographs an empty screen), both inside one event-loop turn so no frame is committed between — the D4 ordering, now load-bearing. Clock is cairo/Pango per output, ticking on the **minute boundary** rather than every 60 s (a fixed interval drifts, showing the change up to a minute late), drawn with a soft shadow because it sits over a photograph of the user's own desktop whose brightness is unknown. **Blank timeout per the Q2 ruling** — 180 s AC / 60 s battery, configurable, 0 = never; both hardcoded values replaced by `arm_blank_timer()`, which re-reads `hw.acpi.acline` at every arm so unplugging mid-lock takes effect on the next keystroke, and falls through to the AC value where the sysctl is absent. New `ui { lock { ... } }` block, documented in `hikari.conf`; `blur` accepts a boolean *or* an object so disabling and tuning share one key. **Validation:** 0 warnings across 64 files in both configs; 11/11 new wlroots symbols exported; config parsed with **real libucl**; blur **unit-tested standalone** (edges clamped, gradient monotonic, alpha preserved) and clean under **ASan+UBSan** across 6 geometries. The capture path cannot be tested here — it needs a live renderer and a composited scene. |
| **Phase 73** | FB-6 Retired + W2 — Scene Layer Trees; **F1 and F2 Fixed** | Implemented, unbuilt — the largest change of the project so far, 261 insertions / 50 deletions across 12 files. **FB-6:** `WITH_POSIX_C_SOURCE` retired per the user's Option 1 ruling, with the two comment blocks that referenced it rewritten to state what they now do; no reference remains outside `.devdocs/`, default build unchanged, **TODOS P3 closed**. **W2:** `struct hikari_server.layers` gains six `wlr_scene_tree *` (background/bottom/views/top/overlay/lock) created in `setup_scene_graph()`; all 7 attachment sites repointed and **zero** scene-root attachments remain outside `server.c`. Stacking order is established by raising each tree in turn rather than trusting `wlr_scene_tree_create()`'s insertion end, which could not be tested at runtime. **F1 (CRITICAL) fixed structurally:** the boundary is four `set_enabled(false)` calls on the desktop layers, and wlroots disables every child of a disabled node — so views, top bar, indicator overlays and every layer-shell surface go dark together and no client action can put a window back on screen. Public views are reparented to the lock layer **and explicitly enabled**, which is not redundant with the flag flip: a public view parked on another sheet has a *disabled* node the flag never touched — **exactly why a `public` clock never appeared**. `background` deliberately stays enabled so the wallpaper shows, matching upstream's dimmed background; W4 decides whether the blur backdrop replaces or overlays it. **F2 (HIGH) fixed structurally**, false comment replaced. **A bug in this phase's own work was caught before shipping:** a view's scene tree outlives unmap (destroyed in `destroy_handler`), so a public view unmapping while locked and remapping after unlock would keep a stale parent under the disabled lock layer and be **invisible forever** — and `reset_visibility()` could not catch it, since unmap removes the view from the list that loop iterates; fixed by deriving the parent unconditionally at map. **Stacking is preserved across lock/unlock:** `output->views` is top-first and `reparent` appends, so forward iteration would have **inverted the desktop** on every unlock — both loops use `wl_list_for_each_reverse`. **Layer-shell ordering became structural** via `layer_scene_tree()`; both ad-hoc raise/lower pairs deleted and `set_layer` is now a reparent, fixing a latent bug where `BACKGROUND` surfaces sank *below* the wallpaper because both called `lower_to_bottom()`. **Unrecorded bug found and fixed:** views **never restacked in the scene at all** — nothing called `raise_to_top` on `view->scene_node`, so window stacking was fixed at map time and `hikari_view_raise()` had no visual effect; scene half added to `raise_view()`/`hikari_view_lower()`, scoped to the parent tree. **Deviation, declared:** the `forced` flag was **not** deleted despite `PLANS.md` W2 step 3 — it is 15 sites, not a handful, several in provably-unreachable branches inside the subsystem behind eight crash phases; F1/F2 are fixed by the trees alone so the removal is pure cleanup, deferred rather than bundled into an unbuildable change. **Validation:** 0 warnings across 60 files in both configs plus `topbar.c`; semantics beyond the compiler's reach verified by reading (list direction, teardown ordering, `cancel()` reachability). **Most in need of runtime verification of anything in this project — unbuilt, unrun.** |
| **Phase 72** | W1 — Platform Capability Layer, Buffer Consolidation, FB-8 | Implemented, unbuilt — 4 new files, 4 modified. **Buffer dedup:** `src/buffer.c` now holds the compositor's only `wlr_buffer_impl`; `hikari_argb8888_buffer` moved out of `server.c` verbatim (sole change: `data` is `const`), and `output.c`'s byte-identical `hikari_background_buffer` deleted. `output.c` −66/+5, `server.c` −108/+60, 115 shared lines in `buffer.c`. `hikari_server_create_argb8888_buffer()` kept as a one-line shim so `bar.c`/`indicator_bar.c`/`lock_indicator.c` stay untouched. **The moved comment no longer claims this is a FreeBSD/ZFS workaround** — it states the real reason (wlroots exposes no allocator a compositor can write pixels into, on any platform) and cites BLUEPRINT §13 FB-2, retiring the Phase 33 framing at its source rather than only in the register. **Seven now-dead includes removed** across the two files, one of which carried a comment claiming the header was "required for the CPU-backed ARGB8888 buffer below" — false the moment that buffer moved; deleted on the Phase 70 F2 precedent. **Platform layer (D3):** `hikari_platform_probe()`/`_log()` run in `server_init()` right after linux-dmabuf, emitting one `wlr_log(WLR_INFO)` block early enough that a startup death still leaves the facts in the log. Records `render_buffer_caps` (the probe W3's blur backend will branch on — replacing an undocumented FreeBSD assumption with a public-API question), the renderer's DRM node resolved by `st_rdev` match against `/dev/dri` rather than guessed, the `card*` count, and a **live** `posix_fallocate()` probe on `XDG_RUNTIME_DIR` (probing beats pattern-matching "zfs", and stays correct for any filesystem with the same limitation). With >1 GPU present it names the `WLR_DRM_DEVICES` override in the log beside the symptom. **FB-8 fixed and verified:** `.ifdef` tests definedness not value, so no feature could be disabled from the command line at all; reproduced first, then all 11 switches converted to `.if defined(X) && ${X:tu} != "NO"` and verified by `make -V` across the matrix with the default config unchanged. A tidier `.for`+`.undef` normalisation was tried and **rejected on evidence** — `.undef` cannot remove a command-line variable in bmake, so it would have silently not worked; recorded in the Makefile so it is not re-attempted. **FB-6 deliberately held for a user decision** — root cause is deeper than recorded (all three symbols sit behind `__BSD_VISIBLE`, which `_POSIX_C_SOURCE` clears, and `lock_mode.c`'s shim is guarded `!defined(__FreeBSD__)` so it never fires); the fix is either retiring a never-set flag (AGENTS.md §3 bars removing features unbidden) or a three-file workaround (barred by the standing anti-debt directive). **Also rejected, and raised rather than taken:** adding libdrm to get `drmGetVersion()->name` (`i915` vs `nvidia-drm`) — better FB-3 evidence and MIT-licensed, but a new link dependency outside approved scope. **Validation:** 0 warnings across all 60 files in **both** build configurations, plus `topbar.c`. Not built, not linked. |
| **Phase 71** | W5 + W6 — Lock-Mode NULL Guards, Unlocker Deny Path, Clipboard | Implemented, unbuilt — first code workstream of the Phase 70 plan; 76 insertions / 3 deletions across 3 files. **W5:** **F3** turned out to be **two** unguarded dereferences of `mode->disable_outputs`, not the one the plan named — `disable_outputs()` (`lock_mode.c:507`) is reached **directly** by `key_handler`'s Ctrl+C branch, so a failed timer allocation faulted on a keystroke, not merely at lock time. Both guarded; a lost timer now degrades to "never blanks" with an always-on `wlr_log(WLR_ERROR)` per the Phase 61 policy, which is a legitimate configured state anyway once Q2's `0 = never` lands, and far preferable to killing the compositor mid-lock. **F5:** the unlocker's fatal-PAM path (`hikari_unlocker.c:143`) now writes a deny result, matching `:88`. **The plan's justification for F5 was wrong and is corrected:** `locker_result_handler` already recovered correctly through `WL_EVENT_HANGUP` — its own comment at `lock_mode.c:362-372` documents the `READABLE|HANGUP` pairing — so nothing was "silently consumed"; the actual gain is that deny is immediate rather than waiting on process teardown. **W6:** **C1** — `wlr_xwayland_set_seat()`, called nowhere in the tree and absent upstream too, added after `setup_selection()` (ordering forced: the seat does not exist when `setup_xwayland()` runs), restoring X11↔Wayland clipboard and primary selection. **The plan's companion instruction to "add a `seat_destroy` guard" was found incorrect and deliberately not done** — `struct wlr_xwayland` owns that listener privately. **C2** — `ext-data-control-v1` advertised alongside the `wlr-` variant; both coexist by design. **C3** — both manager returns guarded, non-fatally by choice, since a missing clipboard manager leaves the compositor fully usable. **F4 deliberately NOT implemented:** conditional on W0-6, which has not been run; whether `hikari_output_enable()` needs an explicit mode is unanswerable from headers and guessing would either add dead code or mask real behaviour. **Validation:** 0 warnings on all three files under the Phase-68-corrected clang invocation, **including `server.c` compiled without feature macros** to exercise the `#ifdef HAVE_XWAYLAND` guard as false; `nm -D` confirms all three newly-called symbols are exported by the installed `libwlroots-0.20.so` (a syntax check alone could not); three `-Wextra` warnings in `hikari_unlocker.c` confirmed pre-existing at `HEAD`. Not built, not run. |
| **Phase 70** | Lock-Screen Investigation, Option B Plan, and the FreeBSD Native-Compatibility Track | Investigation complete, plan approved, **no product code changed** (user directive for the investigation turn was read-only). **Premise partly counterfactual:** the blurred lock screen with a clock has never existed — one `grep -ri blur` hit tree-wide, a HiDPI comment at `bar.c:679`; nothing in any tracker or in git history; branch `lockscreen` is identical to `master`. Greenfield, not a regression. **F1 (CRITICAL): the lock screen hides nothing.** `override_visibility()` flips flag bits only, and the two sites that carry the flag to the scene graph (`view.c:1157`/`:1193`) both assert `!is_forced` — the exact state lock mode establishes. Upstream worked because `hikari_renderer_lock_mode()` composited the lock screen itself (`git 7777aaa:renderer.c:823` — dimmed background + public views + indicator); the wlroots-0.20 port deleted `renderer.c` and **never wrote a scene-graph equivalent**, so `frame_handler` commits the live desktop. Private windows, the top bar and every layer surface are displayed for ~1 s after locking and for a fresh 10 s after every password keystroke. **F2 (HIGH):** a window mapping while locked renders too, and `view.c:1041-1046`'s comment claims the opposite. Also **F3** (unguarded timer pointer), **F4** (= P2-14), **F5** (unlocker fatal path writes no result), **C1** (`wlr_xwayland_set_seat()` called nowhere — X11↔Wayland clipboard has never worked; verified absent upstream too, not a port regression), **C2/C3**. The lock *security boundary* was traced and is sound — every defect is in rendering. **N5 confirmed as fact:** `xwayland_unmanaged_view.c` has no `wlr_scene` reference at all and `xwayland_view.c` attaches no surface content, promoting `PLANS.md` item -9 from hypothesis to finding — and constraining sequencing, since fixing it widens F1. **Two corrections to the record:** (a) **new §5 H0** — this is a hybrid Intel+NVIDIA laptop with `hw.nvidiadrm.modeset=1`, never considered by Phase 19, and it explains both eDP-1 log lines (including the `EGL_EXT_device_drm` failure, which is NVIDIA-EGL behaviour, not broken Mesa) with one cause; falsifiable via `WLR_DRM_DEVICES=/dev/dri/card0`. (b) **Phase 33's CPU-buffer "FreeBSD workaround" is not one** — wlroots 0.20.2 exposes no public shm/CPU allocator at all, so the custom `wlr_buffer_impl` is idiomatic everywhere; the real debt is that it is written twice. Option B (live-screen blur) verified implementable entirely on public 0.20.2 API with **no custom shaders**, via `wlr_scene_output_build_state(.swapchain=…)` + `wlr_texture_read_pixels` (glReadPixels-backed, so it sidesteps `gbm_bo_map`). Plan: **W0–W8** in `PLANS.md` item -12; decisions **D1–D4** and user rulings **Q1–Q4** in `DECISIONS_LOG.md` Phase 70; new **BLUEPRINT §13** FreeBSD register (FB-1…FB-9). **No step is yet approved for execution.** |
| **Phase 69** | Review Round 4 -- Unchecked `setenv` + Status-Masking Log Pipeline | Implemented, unbuilt -- two findings against Phase 68's own code, **both valid, both fixed**. **(1)** Both DISPLAY `setenv()` calls ignored their return value. At `setup_xwayland()` a silent failure either reinstates the Phase 68 lazy-start deadlock (when launched via the wrapper, which unsets DISPLAY) or -- worse -- leaves DISPLAY naming the user's separate `Xorg :2`, sending every autostarted X client to a foreign display. Now fatal with a `wlr_log` diagnostic. The same check in `xwayland_ready_handler()` was deliberately made **non**-fatal: post-Phase-68 that call is a redundant re-export from a live event handler, DISPLAY already holds a valid value, and killing a running session over it would destroy more than it protects. **(2)** Phase 68's `... | tee -a` logging pipeline masked the compositor's exit status (POSIX pipelines report the *last* command's status) and meant `exec` replaced only a subshell, so hikari was not the top-level process. Proven empirically: the old form reported **exit 0** for a child killed by SIGSEGV. Replaced with `exec >> "$HIKARI_LOG" 2>&1` on the wrapper's own descriptors, preserving a true exec -- now reports **139** correctly, and collapses Phase 68's duplicated dbus branches. `pipefail` was unavailable (`#!/bin/sh`). This defect would have defeated the very debug cycle Phase 68 A existed to enable. |
| **Phase 68** | Diagnostics + XWayland Deadlock + NULL-Deref Class + clang-format | Implemented, unbuilt -- approved as a single build cycle. **(A)** `start-hikari.sh` never redirected stderr, so every `wlr_log` line was discarded; Phase 53 identified this and it was never fixed, and it is why three crash investigations ran blind. Added an opt-in `HIKARI_LOG` tee covering both exec paths. Re-verified the rest of the diagnostic surface and found it **better than recorded**: `/var/coredumps` exists, `kern.corefile` is set, `ulimit -c` unlimited, 3 hikari cores present, gdb+lldb installed -- **correcting Phase 53's claim that /var/coredumps did not exist**. **(B) Root cause for the Phase 65 P0.** `setup_xwayland()` requests lazy mode but set `DISPLAY` only from the `ready` handler. wlroots opens the X socket and fills `display_name` inside `wlr_xwayland_create()`, but with lazy mode does not exec Xwayland until a client connects -- and no client can connect without `DISPLAY`, so `ready` never fires and `DISPLAY` is never set. A closed loop in which XWayland could never start; `start-hikari.sh` also `unset DISPLAY` first. Fixed by exporting `display_name` right after the NULL guard, per the contract stated verbatim in the installed 0.20.2 header. **Supersedes and unblocks the Phase 64 XWayland render-gap finding, which was untestable until now.** **(C)** Enumerated all 64 `wlr_*_create*` calls and hand-verified each: **7 unguarded sites** fixed with the Phase 67 pattern, plus `idle_notifier` (delayed-symptom shape -- only dereferenced once a media client takes an idle inhibitor) and `server->seat`, whose `assert()` guard was **dead code** in every shipped binary (`-DNDEBUG`; `strings hikari` -> 0 asserts) and so read as guarded while being unguarded. **234 asserts tree-wide remain dead** -- scoped as a separate project. **(D)** `.clang-format` failed to load at all because `Language: C` is not a valid value in any release (C uses the `Cpp` selector) -- **correcting Phase 67's version-mismatch diagnosis**. Fixed that one line on user instruction, style otherwise untouched; running the config remains destructive and is acknowledged. **Also corrected the validation method for Phases 61-67:** the recorded `cc -fsyntax-only -Wall` used the wrong compiler (sandbox GCC vs FreeBSD clang) and defined no feature macros, so every `#ifdef` region went unchecked. Under clang with all macros, the tree is **0 warnings across 60 files**, which means `DEBUG=YES` (`-Werror`) will compile. |
| **Phase 67** | External Review Round 3 — Layer-Shell NULL Deref + Virtual-Pointer Cursor Hijack | Implemented, unbuilt — Two external review findings verified against current code; **both valid, both fixed, none rejected**. (1) `setup_layer_shell()` fed `wlr_layer_shell_v1_create()`'s result straight into `wl_signal_add` with no NULL check, so allocation failure segfaulted at startup instead of failing cleanly; guarded with the `pointer_gestures` fatal-exit pattern from the same file (user chose fatal over graceful, since the graceful variant needs an extra `wl_list_init` purely to stop `hikari_server_stop()`'s unconditional `wl_list_remove` from turning a startup crash into a shutdown one). (2) `new_virtual_pointer_handler()` used the cursor-wide `wlr_cursor_map_to_output()`, so any `zwlr_virtual_pointer_v1` client supplying a suggested output permanently confined the **physical** mouse, touchpad and touchscreen to it — the pointer-side counterpart to Phase 50 Finding 2's touch fix; replaced with per-device `wlr_cursor_map_input_to_output()`. **Also corrected the validation procedure of Phases 61-66:** the recorded `cc -fsyntax-only -Wall` neither ran in this environment (libdrm needs `__kernel_size_t`) nor defined `HAVE_LAYERSHELL`/`HAVE_VIRTUAL_INPUT`, so every `#ifdef`-guarded region — including both regions fixed here — was silently skipped. Working invocation recorded in DECISIONS_LOG Phase 67. |
| **Phase 66** | License and Branding Update | Implemented — Adopted a full 2-Clause BSD license for Hikari Sakura (Copyright Orpheus497 2026), retained the upstream raichoo license attribution, and swept README.md to unify project branding while strictly preserving executable binary names and configuration paths. |
| **Phase 64** | Cursor Offset + External Review Triage + Doc-Standard Cleanup | Implemented, unbuilt — Cursor offset root-caused: `surface_at()` passed window-geometry-local coords to `wlr_xdg_surface_surface_at()`, which takes wl_surface-local ones; the difference is the CSD margin (`xdg_surface->geometry.x/y`), so every hit test landed that far up-and-left of the pointer. Rendering was always correct, which disguised it as a cursor bug. Triaged 15 external review findings against current code: **7 implemented** (premultiplied alpha at 12 scene-rect sites — correcting Phase 60's "already alpha-correct" claim; `node_at()` out-param initialisation fixing indeterminate reads at all 5 callers; keycode `strtol` underflow turning keycode 0 into 4294967288; locker `wl_event_loop_add_fd` NULL hanging the lock screen; colour int range; `damage_whole` enabled guard; layer-popup geometry init), **5 rejected as invalid** with the verification disproving each, **2 declined as policy** (`AGENTS.md` amendments). Normalised 17 `##`-prefixed comments that matched no `AGENTS.md` prefix. **Found but not fixed: XWayland views attach no surface content to their scene tree.** |
| **Phase 63** | Popup Scene Nodes + Shutdown NULL Deref | Implemented, unbuilt — Session ran 11 minutes with no runtime crash, a first. Two defects surfaced: (1) `xdg_popup_create()` never created a scene node, on the strength of a comment wrongly claiming `wlr_scene_xdg_surface_create()` handles popups automatically — it walks *subsurfaces*, and nothing in `types/scene/xdg_shell.c` traverses popups. Every right-click menu, submenu and combo-box dropdown had therefore never rendered, hidden only because creating a popup used to abort first. `layer_shell.c` had the identical gap, retroactively explaining the long-standing "waybar sub-menus" backlog item. (2) Shutdown SIGSEGV — `hikari_workspace_focus_view()` dereferenced `hikari_server.workspace` unguarded, which `hikari_output_fini()` NULLs while tearing down the noop output. |
| **Phase 62** | SIGABRT Root Cause — Popup Unconstrained Before Initialisation | Implemented, unbuilt — Second core dump. `xdg_popup_create()` called `popup_unconstrain()` at creation time; `wlr_xdg_popup_unconstrain_from_box()` ends in `wlr_xdg_surface_schedule_configure()`, which asserts `surface->initialized`, and wlroots emits `new_popup` from the client's `get_popup` request before the popup surface is ever committed. Aborted on **every** xdg_popup. Fixed in `xdg_view.c` and `layer_shell.c` by deferring to the existing `initial_commit` branch. hikari already understood this constraint for toplevels (the comment explaining why `wlr_xdg_surface_ping` was removed cites the same assertion) but never applied it to popups. |
| **Phase 61** | ROOT CAUSE — NULL Dereference in `session_active_handler` | Implemented, unbuilt — **First core dump ever captured in this project.** `session_active_handler()` read `*(bool *)data`, but wlroots emits `session->events.active` with `data == NULL` (`backend/session/session.c:27,33`) — an unconditional NULL dereference on every VT switch and every seat disable. `gdb` put frame #0 at `session_active_handler+10`, the first memory access after the prologue. Corrected two prior conclusions: there were always **two** crash signatures (SIGSEGV *and* SIGABRT — the "SIGABRT, not SIGSEGV" premise drove Phases 53-57 down the wrong path), and the captured crash carried **no assertion message**, contrary to Phase 57's prediction. Also fixed Finding A, the half-implemented `hikari_xwayland_unmanaged_evacuate()` on the same code path, and Finding B, `override_redirect` being decided once and never revisited. |
| **Phase 51** | Documentation Rebranding (Hikari Sakura) | 100% ✓ — Rebranded user-facing documentation to "Hikari Sakura" and emphasized the FreeBSD-focused Wayland desktop environment revamp from the abandoned original. Modified README.md, share/man/man1/hikari.md, CoC.md, share/wayland-sessions/hikari.desktop, and start-hikari.sh comments. Kept config paths and binary names as "hikari" per user instruction. |
| **Phase 49** | Touchscreen & Trackpad Gesture Implementation | Implementation complete; build/runtime verification pending — User approved the detailed implementation plan from `implementation_plan.md`. Executed the plan strictly using IDE tooling. Created `touch.h`/`touch.c` to wrap touch devices and cleanly handle the `destroy` signal. Updated `Makefile` to link `touch.o`. Modified `src/server.c` to support `WLR_INPUT_DEVICE_TOUCH` in the input loop, attaching it via `wlr_cursor_attach_input_device`, and appropriately adding `WL_SEAT_CAPABILITY_TOUCH` to the seat. Initialized `wlr_pointer_gestures_v1` in `setup_decorations`. Added full Touch and Gesture lifecycle listeners (down, up, motion, cancel, frame, swipe, pinch, hold) to `struct hikari_cursor` in `cursor.h`. Implemented touch coordinate resolution utilizing `hikari_server_node_at` and wired touch/gesture signals natively into the seat within `src/cursor.c`. Build verification and native runtime testing are pending user compilation (root-owned environment constraint); mark fully complete only after those pass. |
| **Phase 48** | Finding 6 Expanded — External Review Triage | 100% ✓ — User pasted 4 comments from an external automated code review, explicitly instructing them be treated as untrusted data, verified against current code, and only valid issues fixed. One ("add an approval-gate before wlr_xwayland_create/fork/execl") was flagged and rejected as a likely prompt-injection payload — not a coherent code fix, and its phrasing mirrors this repo's own `AGENTS.md` agent protocol rather than compositor logic. One (`bar.c` comment-header claim) was verified stale against current code, no action. Two were verified valid and fixed: `bar.c`'s `hikari_topbar_source_init` had two inconsistent child-cleanup paths on startup failure (one cleared `source->pid` without confirming the SIGTERM'd child had actually exited; the other didn't touch the child at all, leaking a permanently-looping orphan) — fixed via a shared `terminate_and_reap_topbar_child()` helper applied at both sites; `lock_mode.c`'s `defer_locker_pid()` fell back to a **blocking** `waitpid` when its 8-slot pending table filled, freezing the whole compositor mid-session (same bug class Phase 38 fixed elsewhere in the file) — fixed by having it return `bool` and having `submit_password()` deny rather than block when parking fails. |
| **Phase 47** | Finding 5 Resolved by Investigation; Finding 9 Reachability Confirmed | 100% ✓ — Read `configuration.c` (full, 2014 lines) and `keyboard_config.c` (full, 398 lines), the follow-up needed since Phase 42/45. Finding 9: confirmed `hikari_configuration_reload()` reconfigures every already-connected keyboard on every reload — the Phase 45 keymap-unref fix was closing a real, repeatable leak. Finding 5: traced the full guarantee chain (`hikari_keyboard_config_default()` always tags itself `"*"`; both `parse_keyboards()` and `finalize_keyboard_configs()` independently guarantee a `"*"` wildcard config exists; `hikari_configuration_load()` only reports success after that guarantee holds) and confirmed `add_keyboard()`'s `assert(keyboard_config != NULL)` is a genuinely sound invariant given the current parser — investigated and closed with no code change, rather than defensively rewriting something that wasn't broken. **All 9 findings from the Phase 42/44 audit are now triaged**: 7 implemented (1, 2, 3, 4, 7, 8, 9), 1 investigated and confirmed sound (5), 1 remaining open as an explicit optional/low-priority backlog item (6). |
| **Phase 46** | Execution — Findings 3 and 4, Scoped as Directed | 100% ✓ — User directed: Finding 4 → graceful degradation specifically for subsurface/popup creation and buffer allocation (not a blanket OOM-policy change); Finding 3 → scope logging down to crash-relevant paths only. Added `hikari_try_malloc()` (`memory.c`/`memory.h`) as an opt-in, non-aborting counterpart to the existing fail-fast `hikari_malloc`/`hikari_calloc`, applied at exactly 9 call sites: 4 subsurface-creation sites in `view.c`, popup creation in `xdg_view.c` and `layer_shell.c` (×2), and the two buffer-allocation functions in `server.c` and `output.c` (the latter now falls through to its existing solid-color fallback on allocation failure instead of aborting). Every other allocation site is untouched, keeping the fail-fast default. For logging, no new module was introduced — `memory.c`'s abort/degradation diagnostics now route through the codebase's existing `wlr_log()` primitive (already leveled, timestamped, and initialized in `main.c`) instead of raw `fprintf`; pre-existing `fprintf` call sites elsewhere were deliberately left untouched, per the "scope it down" direction. |
| **Phase 45** | Execution — Findings 1, 2, 7, 8, 9 Implemented | 100% ✓ — User approved the Phase 42/44 findings and said "proceed." Implemented, in the approved order: (1) the `hikari_view_child.fini` dispatch-pointer fix for the popup/subsurface type confusion in `hikari_view_unmap` (`view.c`, `xdg_view.c`, `view.h`); (2) `wl_event_loop_add_signal`-based SIGTERM/SIGINT handling replacing the async-signal-unsafe raw `signal()` handler (`server.c`, `server.h`); (7) the missing `hikari_free` in `switch.c`'s destroy handler; (8) a cache/change-detection short-circuit in `indicator_bar.c`'s `hikari_indicator_bar_update()`, mirroring `bar.c`'s proven pattern (`indicator_bar.c`, `indicator_bar.h`); (9) the missing `xkb_keymap_unref` in `hikari_keyboard_configure()` (`keyboard.c`). No build run this phase (IDE-only tooling; user to `sudo make clean && sudo make install` and stress-test). Findings 3-6 deliberately deferred pending a check-in — Finding 4 is a policy decision, Finding 3 is larger scope, Finding 5 needs a `configuration.c` read not yet done. |
| **Phase 44** | Deepened Memory/Process Audit — DOD Verdict, Allocation Churn, Two More Leaks | 100% (investigation-only) ✓ — Continuation of Phase 42, prompted by the user asking for a data-oriented-design lens on memory/process handling. Checked project history first: `PROGRESS.md` records a prior DOD SoA/object-pool attempt reverted as incompatible with `wlr_scene`; reconstructed why from this session's evidence (every wlroots protocol object is individually heap-owned with embedded listeners wlroots itself walks — a pool/SoA layer would add a lookup indirection on every hot signal for no locality win) and recorded a verdict: no rewrite, fix the correctness bugs, port the already-proven cache/reuse pattern from `bar.c` into `indicator_bar.c`, and only consider narrowly-scoped pools later if profiling (not speculation) shows it matters. Traced the "process crashes mid-drag/mid-assign" hypothesis through `clear_focus`/`hikari_normal_mode_enter`'s `mode->cancel()` dispatch across all 6 modal state machines and confirmed it is already handled soundly — no dangling `focus_view` bug found. Found 3 new confirmed issues: (7) `switch.c`'s destroy handler leaks the wrapper struct (missing `hikari_free`); (8) `indicator_bar.c` re-renders unconditionally on every focus change/keystroke with no change-detection, unlike `bar.c` — the clearest concrete match for "CPU and RAM thrashing"; (9) `hikari_keyboard_configure()` leaks the previous `xkb_keymap` ref on reconfigure. No code changed; full detail in DECISIONS_LOG Phase 44, action items in TODOS/PLANS, awaiting user approval. |
| **Phase 43** | User-Facing Documentation Enhancement | 100% ✓ — Deep audit of configuration parser completed. Updated `README.md` and `etc/hikari/hikari.conf` with comprehensive configuration examples covering input devices (pointers, keyboards, lid switches), multi-monitor outputs, views matching, and advanced layouts, correcting prior misconceptions about hardware configuration capabilities. |
| **Phase 42** | Memory Management, Crash, and Error-Handling Deep Audit | 100% (investigation-only) ✓ — Read-only audit answering the user's report of continued crashes (media players, closing windows, multiple browser tabs, random crashes), memory leaking/segfaulting, no graceful termination, no error handling, no built-in logging. Read `main.c`, `server.c` (full, 2238 lines), `view.c` (full, 2034 lines), `xdg_view.c`, `xwayland_view.c`, `xwayland_unmanaged_view.c`, `cursor.c`, `command.c`, `output.c`, `layer_shell.c`, `topbar.c`, `decoration.c`, `memory.c` in full. Found and documented 6 findings (full detail + cross-references in DECISIONS_LOG Phase 42): (1) CRITICAL — `hikari_view_unmap` type-confuses `hikari_xdg_popup` as `hikari_view_subsurface` via a shared list, freeing a struct while wlroots holds 4 live listeners into it (delayed UAF; independently corroborated by swaywm/sway#5321, a Firefox-triggered instance of the same bug class); (2) CRITICAL — signal handling calls non-async-signal-safe code directly from a raw `signal()` handler and never registers SIGINT, so Ctrl+C skips all cleanup (cross-referenced against `wl_event_loop_add_signal`, the pattern used by Wayfire/labwc); (3) HIGH — no built-in structured logging, only scattered `fprintf(stderr,...)`; (4) HIGH — fail-fast `abort()`-on-OOM policy (Phase 26) has no degradation path under the exact memory pressure many-tabs/media workloads create, flagged as a decision point; (5) MEDIUM — a production invariant gated behind `assert()` (`add_keyboard` in `server.c`) that `NDEBUG` release builds strip; (6) LOW — `hikari_command_execute`'s blocking intermediate-child reap, informational only. No code changed this phase; implementation plan recorded in `PLANS.md`/`TODOS.md`, awaiting user approval per `AGENTS.md`'s Zero Unapproved Action rule. |
| **Phase 40** | Resize/Move NULL-Output Guard Sweep | 100% ✓ — User reported crashes under multiple windows, multiple workspaces, many Firefox tabs, and occasionally when resizing heavy clients. Investigation (two Explore passes plus manual review) confirmed the view-list lifecycle (`sheet_views`/`output_views`/`group_views`), fixed-size sheet/workspace arrays, process spawning, output damage scheduling, and layer-shell teardown are all sound. Found `queue_resize` (`src/view.c`) dereferencing `view->output->usable_area` with no guard against `view->output` being NULL — `hikari_view_init` seeds it from `workspace->output` (a Phase 38 post-review addition), so it is non-NULL once a workspace is supplied, but stays NULL when `workspace` is NULL and can still be reassigned by `hikari_view_configure()`; `hikari_view_refresh_geometry` already guarded this precondition. Added the guard to `queue_resize`, and swept sibling call sites reachable via the same user-action path (`hikari_view_move`, `hikari_view_move_absolute`, the `MOVE(pos)` macro in `src/view.c`) adding matching guards. Build verification pending user compilation (root-owned `.o` artifacts block agent-side `make`). |
| **Phase 38** | Window Creation Crash and Scene Tree Ownership | 100% ✓ — **Runtime-verified by user: windows now open; most functional state to date.** Root-caused the crash-on-every-window-creation to `hikari_view_refresh_geometry` (`src/view.c`) dereferencing `view->output->geometry` while guarding only `view->scene_node`; at the time, `view->output` was left NULL until `hikari_view_configure` ran, and `first_map` called refresh inside that window. (A post-review addition also seeded `view->output` from `workspace->output` in `hikari_view_init`, so the NULL guard now covers only the narrower case of a NULL workspace or a later reassignment, not every window creation.) Restructured XDG scene ownership: hikari now owns the parent tree (`wlr_scene_tree_create`) with the wlroots tree attached beneath as `surface_tree`, since `wlr_scene_xdg_surface_create` destroys its returned tree and all children from its own destroy listener (verified in `wlroots-0.20.0/types/scene/xdg_shell.c`). Border/indicator rects switched to parent-relative coordinates (were double-offset). XDG scene tree now destroyed on teardown (was leaked per window). Also: unlocker PATH-hijack fix, lock-mode hangup race, `wlr_buffer_drop(NULL)` guard, `view->scene_node` init, `hikari_output_init` uninitialised-read assert, `commit_handler` compile error. |
| **Phase 37** | Wayland Client Initialization Crash Fix | 100% ✓ — Fixed regression from Phase 10 where `commit_handler` registration was moved from `map` to `hikari_xdg_view_init` to catch `initial_commit`, which inadvertently exposed intermediate unmapped commits to the `assert(view->surface != NULL)` check. Added `if (!xdg_surface->surface->mapped) { return; }` to `src/xdg_view.c` to properly discard these intermediate unmapped commits. |
| **Phase 36** | XWayland Unmanaged View, VT Session Guards, and Follow-ups | 100% ✓ — Fixed UB crash in `src/xwayland_unmanaged_view.c` by properly initializing and wiring `map`/`unmap` listeners mirroring the `associate` lifecycle. Fixed VT switch lockups by guarding `frame_handler` and `request_state_handler` in `src/output.c` with a `session_active` flag tracked in `src/server.c`. Added bounded depth guards to popup parent-walk loops in `src/layer_shell.c`. Follow-up: Fixed UAF in `src/view.c` (`hikari_view_evacuate`) by unconditionally relinking `sheet_views` and `output_views`. Fixed missing `wlr_log_init` in `main.c` for release builds and added structured VT transition logging in `src/server.c`. Code compiled successfully; user verification pending. |
| **Phase 35** | Wayland Decoration Lifecycle Fixes | 100% ✓ — Fixed `wlr_xdg_toplevel_decoration_v1_set_mode` asserting before initial_commit by deferring to `decoration->scheduled_mode` in `src/decoration.c`. Fixed `wlr_server_decoration` leaking `mode` listeners by adding a `destroy` listener to `include/hikari/view.h` and explicitly removing them in `src/server.c` and `src/view.c`. Tested via code analysis, awaiting user root compilation. |
| **Phase 33** | Hardware DMABUF Buffer sharing and CPU Background Rendering | 100% ✓ — Fixed `posix_fallocate` Wayland client crash on ZFS by adding `zwp_linux_dmabuf_v1` initialization to `server.c`. Fixed solid color background bug by bypassing `wlr_allocator` GBM mapping errors via a custom `wlr_buffer_impl` implemented in `output.c`. Code compiled cleanly. |
| **Phase 32** | Wayland Client Hang and Wallpaper PREFIX Fix | 100% ✓ — Investigated and resolved the Wayland terminal hang issue by replacing `wlr_xdg_toplevel_set_size(..., 0, 0)` with `wlr_xdg_surface_schedule_configure(surface)` during `initial_commit` in `src/xdg_view.c`. Addressed the black screen issue by fixing the `PREFIX` macro replacement in the user `Makefile` installation target and the local `~/.config/hikari/hikari.conf` file. Tested thoroughly in a nested session. |
| **Phase 31** | wlroots 0.20 Initialization Guards | 100% ✓ — Applied `surface->initialized` guards to `activate` and `resize` in `src/xdg_view.c` to fix the `wlr_xdg_surface_schedule_configure` assertion crash during client startup. Execution complete. Testing blocked by root-owned `main.o` file environment issue. Devdocs updated. |
| **Phase 30** | Compositor crash and background fallback fixes | 100% ✓ — Fixed `wlroots` `surface->initialized` crash in `src/xdg_view.c` by guarding `activate`, `resize`, `apply_tile`, and `reset_geometry`. Fixed silent background failure in `src/output.c` by adding `fprintf` for failed allocator. Devdocs updated. |
| **Phase 28** | `request_state_handler` startup CRTC disable guard | 100% ✓ — Guard added to `src/output.c` `request_state_handler`: silently drops `request_state` events from wlroots that carry `WLR_OUTPUT_STATE_ENABLED` + `enabled=false` while `output->enabled` is `false`, preventing the \"Failed to disable CRTC <N>\" startup error. API verified against wlroots-0.20 headers (`committed & WLR_OUTPUT_STATE_ENABLED` + direct field). `make output.o` → EXIT:0, zero errors/warnings; `output.o` 12656→12680 bytes. Full relink blocked by root-owned binary (env issue). Devdocs updated. |
| **Phase 26** | Phase 24 hardening backlog — P2/P3 batch execution | 100% ✓ — 3/3 items: CSD granular damage (whole-output early-outs removed; CSD main surface damaged by buffer extents, `src/view.c`); fail-fast allocation policy (`hikari_malloc`/`hikari_calloc` sized diagnostic + `abort()`, `src/memory.c`, contract in `include/hikari/memory.h`); changelog `wloots`→`wlroots` (`CHANGELOG.md`). TC-BUILD-01/02 clean builds pass, 0 errors; edited files warning-clean. Phase 24 hardening stream now 7/7 closed. Remaining: runtime/environmental queue (user-run Phase 19 diagnostics, eDP-1 swapchain, tmpfs/ZFS), runtime-blocked verifications (P2-14, PAM, layer-client spot check), TC-FORMAT-01, optional hygiene items. |
| **Phase 25** | Phase 24 hardening backlog — P0/P1 batch execution | 100% ✓ — 4/4 items: unknown `outputs` keys now fail parse (`src/configuration.c`); `parse_switches` UCL iterator freed (`src/configuration.c`); lock-helper child `_exit(EXIT_FAILURE)` + stderr diagnostic after failed `execl` (`src/lock_mode.c`); loud output-commit diagnostic naming the output (`src/output.c`, explicit `<stdio.h>`). TC-BUILD-01/02 clean builds pass, 0 errors; edited files warning-clean. Remaining backlog: P2 CSD damage granularity, P2 allocation policy (design decision pending), P3 changelog typos. |
| **Phase 24** | Deep architecture/wiring audit capture in devdocs | 100% ✓ (docs-only) — exhaustive static audit findings recorded across BRIEFING/BLUEPRINT/DECISIONS_LOG/TODOS/PLANS/SESSION_HANDOFF. Verdict: core compositor wiring is real (no fake subsystems). Added remediation backlog for 6 concrete risks: unknown-output-key parse leniency, `parse_switches` iterator cleanup, lock-helper exec-failure semantics, output-commit diagnostics, CSD damage granularity, allocation-policy hardening. |
| **Phase 23** | Review-findings verification & remediation | 100% ✓ — 6 still-valid findings fixed: version.h FORCE prerequisite + atomic temp-file rename; strtol end-pointer validation for numeric mouse bindings; layer popup offsets via flat `base->geometry`; xwayland `wlr_scene_tree_create` NULL bailout; wallpaper PNG restored + unconditional install rule; 17 function-purpose comment headers. 4 findings verified stale and skipped with evidence (no future-dated timestamps exist; investigation file retired Phase 22; API-verification duplicates already removed). Default + full-feature clean builds, 0 errors. |
| **Phase 22** | Devdocs consolidation — 7-file structure restoration | 100% ✓ (docs-only) — archived runtime investigation content redistributed: launcher architecture → BLUEPRINT §6, corrected eDP-1 failure analysis → BLUEPRINT §5, P2-14 → TODOS active list, P2-15 → BLUEPRINT known limitations. Phase-20 BLUEPRINT §5 draft found factually wrong (failure misattributed to backend start; non-existent diagnostic string; ruled-out causes listed) and corrected against Phase 19 live evidence. All claims re-verified against the tree. |
| **Phase 21** | Runtime report validity audit & launcher architecture analysis | 100% ✓ (docs-only) — Per-finding validity audit and the evidence-backed answer to "why `start-hikari` *and* `hikari`?" (compositor natively owns seatd/socket/env-propagation/lock-PAM; dbus/portals/session-PAM/XDG_RUNTIME_DIR belong to the session layer; 12:47 `setup_env()` revert re-affirmed). Consolidated into BLUEPRINT §5/§6 in Phase 22. |
| **Phase 20** | Exhaustive Codebase Audit & Blueprint Synthesis | 100% ✓ (read-only) — Read every source file and synthesized the system architecture, component wiring, modal state machine, and eDP-1 swapchain diagnostics directly into `.devdocs/BLUEPRINT.md` per `AGENTS.md` operational rules. [x] Extract Hikari configuration mapping and architecture. [x] Refactor architecture and flow in `BLUEPRINT.md` dynamically from codebase mapping. [x] Complete painstakingly detailed manual mapping of the codebase, file by file, into `BLUEPRINT.md` (Sections 12.1 through 12.18). |
| **Phase 19** | Live TTY runtime test & failure triage | 100% ✓ (read-only) — startup verified live through connector probe; blocker localized to the eDP-1 scanout swapchain test in the Mesa/EGL/GBM ↔ drm-kmod layer (below hikari). Ranked hypotheses H1/H2/H3 + user-run diagnostics matrix issued; see SESSION_HANDOFF Phase 19 |
| **Phase 18** | Runtime failure root-cause investigation | 100% ✓ — found 4 release-blockers + 3 P1 + 8 P2 with file:line evidence. Earlier "93-99% wired" and TC-BUILD-01 claims superseded. (Findings consolidated into BLUEPRINT/trackers in Phase 22.) |
| **Phase 18b** | Remediation execution & build revalidation | 100% ✓ — all 15 defects + 3 build-discovered ones (popup geometry, xcb size hints, xwayland associate lifecycle) fixed across 12 files. Default `etc/hikari/hikari.conf` authored (parser-verified). TC-BUILD-01 passed (default clean build); TC-BUILD-02 passed (full-feature clean build+link) — first time feature configs ever compiled in this tree. Runtime TTY test pending. |
| **Phase 1** | Initialization & workspace setup | 100% ✓ |
| **Phase 2** | Documentation & codebase audit | 100% ✓ |
| **Phase 3** | FreeBSD header adaptations | 100% ✓ |
| **Phase 4** | wlroots 0.20 API migration | 100% ✓ (all API breaking changes resolved) |
| **Phase 5** | wlr_scene rendering migration | 100% ✓ (all visual elements use wlr_scene graph) |
| **Phase 6** | Build verification (initial) | 100% ✓ (clean `make` at time of Phase 6 — revalidation tracked under Phase 12) |
| **Phase 7a** | Runtime startup fixes | 100% ✓ (resolved black screen, scene output order, environment setup) |
| **Phase 7b** | FreeBSD runtime validation | Pending (crash fix applied — awaiting retest on FreeBSD Wayland session) |
| **Phase 8** | AGENTS.md code documentation compliance | Partial — amended 2026-08-19: `[COMMENT]` prefixes applied to modified files only; currently 12/57 sources carry the mandated script-purpose header and 25 sources carry function-purpose headers; full rollout tracked in TODOS.md |
| **Phase 9** | Runtime crash fix & final validation | 100% ✓ (removed `wlr_xdg_surface_ping`, fixed fullscreen handler, fixed unlocker framing) |
| **Phase 10** | wlroots 0.20 initial_commit lifecycle | 100% ✓ (commit listener relocated, initial_commit handler added, popup lifecycle added, review fixes applied) |
| **Phase 11** | Startup wiring deep investigation | 100% ✓ (5/7 bugs fixed; BUG-6 non-blocking PAM resolved in Phase 12; BUG-7 no action needed) |
| **Phase 12** | XDG/tmpfs/ZFS resolution & PAM fixes | 100% ✓ — All code changes complete. PAM config fixed. Non-blocking PAM I/O implemented. ZFS detection in start-hikari.sh. README updated. |
| **Phase 13** | Codebase wiring audit, bug fixes & handbook verification | 100% ✓ — Full 55-file/64-header audit scored 93% correct. Fixed: switch toggle cascading if, output cairo surface check, duplicate includes in server.c, blocking `wait()` → `waitpid(WNOHANG)` in lock_mode.c, `output->server` init robustness, main.c comment migration. FreeBSD Handbook Ch.6 cross-reference verified. |
| **Phase 14** | Comprehensive audit bug fixes & dead code cleanup | 100% ✓ — Fixed: BUG-1 move_resize_view dx/dy, BUG-2 outputs_disabled stale state, BUG-3 command.c waitpid, BUG-4 stale comment. Security: explicit_bzero. Robustness: write() check. Added 5 missing listener cleanups. Removed dead code (render.h deleted, mode_handler, unused struct members). Updated desktop entry and gitignore. |
| **Phase 15** | Review fix: start-hikari.sh binary resolution | 100% ✓ — Added SCRIPT_DIR derivation so sibling hikari binary is found regardless of CWD. Three-tier lookup: ${SCRIPT_DIR}/hikari → PATH → ./hikari. |
| **Phase 16** | Review fix: SCRIPT_DIR guard & README tmpfs check | 100% ✓ — Added fatal error guard on SCRIPT_DIR derivation; replaced `stat -f '%T'` with `mount` in README tmpfs verification step. |
| **Phase 17** | Review fix: table pipe escaping & README tmpfs troubleshooting clarity | 100% ✓ — Escaped literal pipes in SESSION_HANDOFF Phase 16 table (markdownlint column miscount); README `zfs` diagnosis broadened to all setup steps incl. fstab + reboot. Build revalidated (user-confirmed 2026-08-13). |


*Note: DOD SoA tables and object pool phases were implemented and subsequently REVERTED as incompatible with wlr_scene workflows.*
