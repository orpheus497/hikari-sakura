# Hikari Project Briefing

*Last Updated:* 2026-08-22 14:46

## Current Status

- **Phase:** Phase 81 (**portal-wlr adopted as the supported screen-sharing path; OBS ScreenCast left open. Session-end documentation pass, no code changes**). User decision recorded: xdg-desktop-portal-wlr is the backend; alternative capture routes are not to be pursued. **Every part of the chain the compositor owns is verified working** — `grim` captures 3840×1200 with 95% non-black samples, `XDG_CURRENT_DESKTOP=Hikari Sakura:wlroots` is present in all four session processes, portal-wlr now activates and stays running, and PipeWire is up. The portal negotiates, the picker appears, an output can be selected — and OBS still renders black. **The residual failure is downstream, in portal-wlr → PipeWire → OBS, and is not this project's code.** Deliberately *not* asserted as an OBS bug: which of the two is at fault has not been established, and doing so needs portal-wlr TRACE captured during an active capture session (the earlier attempt could not acquire the D-Bus name because the activated instance held it). **Hypothesis to carry forward:** hybrid-GPU dmabuf, i.e. FB-3 — PipeWire negotiates dmabuf with OBS, and on Intel+NVIDIA a buffer allocated on one GPU and imported on the other gives exactly this symptom. `force_mod_linear=1` governs only portal-wlr's own allocation, not the PipeWire→OBS handoff, consistent with it not helping. **Resolving FB-3 via the W0 matrix may fix this as a side effect.**
- **Phase:** Phase 80 (**ext-image-copy-capture made opt-in — my Phase 78 change was causing the black capture; awaiting rebuild**). Proven, not inferred: `grim` captures correctly against the live session (so `wlr-screencopy` works), portal-wlr's own TRACE log says **`wayland: using ext_image_copy_capture`**, and hikari advertises that protocol only because Phase 78 added it. **Phase 78 moved portal-wlr off a working path onto a black one** — `grim` was unaffected because it binds screencopy directly and never sees the choice. Not a regression from a working state (screen sharing was also broken before Phase 78, for a different reason fixed in the same phase), but a self-inflicted obstacle with a foreseeable failure mode: **advertising a protocol changes client behaviour, and a newer protocol is not automatically a better one on a given machine.** Now behind `WITH_EXT_IMAGE_CAPTURE`, default off and excluded from `WITH_ALL`, so it can be re-tested with one `make` argument once FB-3 (hybrid-GPU device selection) is resolved. `force_mod_linear=1` was applied and did not help. **Correction to advice I gave:** `dmabuf_device` is an internal variable inside portal-wlr, not a config key — I read it from a `strings` dump and presented it as an interface without checking.
- **Phase:** Phase 79 (**OBS screen sharing diagnosed — a compositor bug, not an OBS issue; fixed, awaiting rebuild**). W8 confirmed working on hardware. Phase 78's portal fix verified live (`XDG_CURRENT_DESKTOP=Hikari Sakura:wlroots` in all session processes). **The real blocker: `WAYLAND_DISPLAY` never reaches the D-Bus activation environment.** `start-hikari.sh` runs `dbus-run-session` *before* the compositor creates its Wayland socket, and D-Bus hands every service it activates the environment the bus itself started with — so `xdg-desktop-portal-wlr` activates with no idea which compositor to connect to, fails, and the portal reports no ScreenCast provider. Verified absent in `dbus-run-session`, the session `dbus-daemon` and the running portal. **Nothing logs this**, which is exactly why it presents as a client problem. Fixed with `export_activation_environment()`, run after the backend starts so `DISPLAY` is published too, guarded by `command -v` and routed through the detached autostart helper so a missing dbus package costs only this feature. **A second blocker is not a code defect:** PipeWire and WirePlumber are installed but not running, and the portal's ScreenCast delivers frames over PipeWire — deliberately not hardcoded, since starting a media daemon is session policy and hikari already provides `~/.config/hikari/autostart` for it. **Also recorded: a diagnostic error of mine** — an initial reading appeared to show `XDG_CURRENT_DESKTOP=Hikari` and cost ~10 minutes of false hypotheses, because `procstat -e` prints a space-separated environment and `tr ' ' '\n'` split the value at its space. A measurement artifact presenting as a plausible bug, in the same investigation as a real one.
- **Phase:** Phase 78 (**W7a + W8 IMPLEMENTED — XWayland renders content for the first time; awaiting the user build**). **W7a:** both capture generations advertised (`ext-image-copy-capture` + `ext-image-capture-source` beside the deprecated `wlr-screencopy`), and the portal fix — verified against the installed `wlr.portal`, whose `UseIn=wlroots;sway;…` matched **none** of `Hikari Sakura`, so screen sharing had no backend at all regardless of protocols; now `XDG_CURRENT_DESKTOP="Hikari Sakura:wlroots"`. **W8:** managed X11 windows attached only border and indicator rects and so drew as empty rectangles, while override-redirect surfaces had **no `wlr_scene` reference whatsoever** — menus, tooltips and drag icons were hit-tested but invisible. Both now attach via `wlr_scene_subsurface_tree_create()` on `associate`. **Ownership is shared with wlroots and handled without betting on a contract:** each registers a destroy listener that nulls its pointer, so neither a double-destroy nor a stale pointer is reachable whichever side goes first — the direct application of the Phase 76 lesson. All 19 listeners audited for exactly-once removal, and destroy ordering verified. **W7b (foreign-toplevel) is deliberately sequenced to the next cycle:** it is *not* required for screen sharing (checked — portal-wlr captures outputs, no window picker), it needs six touch points in `src/view.c` (the file behind eight crash phases), and bundling it would make an X11 crash ambiguous. Sequencing, not a scope cut.
- **Phase:** Phase 77 (**THE LOCK SCREEN WORKS — confirmed on hardware. Clock raised 1 cm; awaiting a rebuild to see it**). The native blurred lock screen with a compositor-drawn clock is delivered: the workspace is captured and blurred at lock time, the clock and date are drawn by the compositor with no client involved, the password indicator and any `public` views layer above it, and blanking is power-aware. **This closes W3, W4 and the original Phase 70 request.** The clock is now raised by a **real centimetre** rather than a pixel guess — `mm_to_logical_pixels()` derives the offset from EDID's `phys_height` against the mode's pixel height and divides by the output scale, since scene nodes are positioned in logical coordinates; it falls back to the 96 DPI convention (10 mm = 37 logical px) where EDID reports no physical size. The existing 40 px indicator clearance is untouched, so this is literally 1 cm higher rather than a re-derived layout. **Took three build cycles, and both failures were mine:** Phase 75 fixed crash 1 correctly but shipped a *speculative* second change that was backwards; Phase 76 found that crash 2 had the same root cause as crash 1 — hand-building a wlroots struct instead of using its constructor — and reverted the speculation. Lessons recorded in DECISIONS_LOG 74–77: a fallback ladder is only as safe as its first rung, because an assertion in a dependency is not recoverable; do not hand-construct a library's structs when it ships a constructor; and **never ship speculation in the same edit as an evidence-backed fix**, because the fix lends the guess credibility it has not earned.
- **Phase:** Phase 76 (**Second crash root-caused and fixed; awaiting rebuild**). The Phase 75 fix was built and still aborted. `hikari.4102.1001.core` named it: **`render/drm_format_set.c:144: assert(src->len <= src->capacity)`** — Phase 75 set `.len = 1` and left `.capacity = 0`. **Two crashes, two different invariants of the same hand-constructed `struct wlr_drm_format`**, whose own header documents `capacity` as *"do not use"*. Patching one field per crash was treating symptoms; the defect was building the struct at all. It is now constructed through `wlr_drm_format_set_add()`/`_get()`/`_finish()`, which keeps the invariants itself — no hand-built format or `.capacity` assignment remains anywhere in `src/`. **Also a correction:** Phase 75's other change, from `wlr_output_transformed_resolution()` to `wlr_output->width/height`, was **backwards and is reverted** — the library's own assert `buffer->width == resolution_width && ...` uses wlroots' naming for the transformed resolution, so rotation is baked into the rendered buffer. That change was speculative, shipped in the same edit as an evidence-backed fix, and took unearned credibility from it.
- **Phase:** Phase 75 (**Phase 74 crash ROOT-CAUSED from a core dump and FIXED; awaiting rebuild**). The user built Phase 74 and the compositor crashed on locking. `hikari.26797.1001.core` gave a clean backtrace to `render_output_offscreen()` and, by disassembling the asserting call site, the exact assertion: **`render/allocator/gbm.c:66: assert(format->len > 0)`**. wlroots' GBM allocator **requires a non-empty modifier list**; Phase 74's format ladder used `len = 0, modifiers = NULL` for its first rung intending that to mean "implicit modifier", which is simply invalid — so the compositor aborted on the very first lock attempt, before ever reaching the LINEAR rungs that were well-formed. Correct spelling is a one-entry list holding `DRM_FORMAT_MOD_INVALID`; all four rungs now carry exactly one modifier. **A second bug was fixed while investigating** — the swapchain was sized with `wlr_output_transformed_resolution()`, but the scene renders in the output's *untransformed* orientation, so a rotated output would have got swapped dimensions; an unrotated laptop panel could never have shown it. **Design lesson recorded:** an escalation ladder recovers only from *returned* failures, never from assertions — a fallback chain is only as safe as its first rung, and Phase 74's read as defensive code while containing a guaranteed abort. Neither bug was reachable by any check available in the agent environment; the core dump found the first one immediately, which vindicates the Phase 68 diagnostics work.
- **Phase:** Phase 74 (**W3 + W4 IMPLEMENTED — the native blurred lock screen with a clock; awaiting the user build**). 6 new files, 8 modified. **Phase 73 was confirmed working on hardware by the user**, who then corrected a requirement that had been mis-scoped since Phase 70: the clock is meant to be a **native compositor function**, not a client marked `public`. The `public` mechanism still works and is retained, but it is no longer the answer — the compositor now draws both the blur and the clock, so they are present with no session running, survive a client crash, and cannot be impersonated by a window that merely looks like a clock. **Capture** renders the output off-screen via `wlr_scene_output_build_state()`'s `swapchain` option and reads back with `wlr_texture_read_pixels()`, which is glReadPixels-backed and so sidesteps `gbm_bo_map` entirely. **The format spike is resolved** as a logged 4-rung escalation ladder. **Two bugs were caught before shipping:** an idle desktop has no pending damage, so the capture would have worked only while something was animating — the worst possible failure shape, fixed with `wlr_output_update_needs_frame()`; and `box_blur_line()` shared one stride between source and destination, which would have overrun the heap by roughly the image height on the vertical pass. **Ordering is load-bearing:** the capture runs before the desktop layers are disabled, both inside one event-loop turn. The clock ticks on the **minute boundary** (a fixed 60 s interval drifts) and is drawn with a shadow, because it sits over a photograph of the user's own desktop whose brightness is unknown. **Blank timeout is now 180 s on AC / 60 s on battery per your Q2 ruling**, configurable, `0` = never, with the power source re-read at every arm. New `ui { lock { ... } }` config block, documented in `hikari.conf`. **Validation:** 0 warnings across 64 files in both configs; config parsed with real libucl; the blur **unit-tested standalone and clean under ASan+UBSan**. The capture path needs a live renderer and cannot be tested here.
- **Phase:** Phase 73 (**FB-6 retired + W2 implemented — F1 and F2 FIXED; awaiting the user build**). The largest change of the project so far: 261 insertions / 50 deletions across 12 files. **FB-6** retired per your Option 1 ruling (`WITH_POSIX_C_SOURCE` gone, TODOS P3 closed, default build unchanged). **W2** gives the scene six named layer trees — `background`/`bottom`/`views`/`top`/`overlay`/`lock` — and **zero** attachments to the scene root remain outside `server.c`. **F1 is fixed structurally:** the boundary is four `set_enabled(false)` calls on the desktop layers, and wlroots disables every child of a disabled node, so the view layer, top bar, indicator overlays and every layer-shell surface go dark together — no client action can put a window back on screen. Public views are reparented onto the lock layer **and explicitly enabled**, which is not redundant with the old flag flip: a public view parked on another sheet has a *disabled* scene node the flag never touched, **which is exactly why a `public` clock never appeared**. **F2 is fixed structurally** and its false comment replaced. **Three things surfaced during implementation that the plan did not have:** (1) a bug in this phase's own work — a view's scene tree outlives unmap, so a public view unmapping while locked and remapping after unlock would have kept a stale parent under the disabled lock layer and been invisible forever; fixed by deriving the parent unconditionally at map. (2) `output->views` is top-first while `reparent` appends, so the obvious forward iteration would have **inverted the desktop on every unlock**; both loops use `wl_list_for_each_reverse`. (3) **Views never restacked in the scene at all** — nothing ever called `raise_to_top` on `view->scene_node`, so window stacking was frozen at map time and `hikari_view_raise()` had no visual effect whatsoever; fixed in `raise_view()` and `hikari_view_lower()`. Also fixed for free: layer-shell `BACKGROUND` surfaces used to sink *below* the wallpaper. Validation: 0 warnings across 60 files in both configs. **Unbuilt and unrun — this is the change most in need of runtime verification in the project's history; the test list is TODOS Phase 73.**
- **Declared deviation from the plan:** the `forced` flag was **not** deleted (PLANS W2 step 3). It is 15 sites rather than the handful assumed, several in provably-unreachable branches inside the subsystem behind eight crash phases. F1/F2 are fixed by the trees alone, so the removal is pure cleanup and is tracked separately rather than bundled into a change that cannot be built.
- **Phase:** Phase 72 (**W1 IMPLEMENTED, awaiting the user build; two decisions tabled**). 4 new files, 4 modified. **The duplicate `wlr_buffer_impl` is gone** — `src/buffer.c` is now the compositor's only one, with `output.c` −66/+5 and `server.c` −108/+60; `hikari_server_create_argb8888_buffer()` remains a one-line shim so the three cairo-drawing callers stay untouched. The moved comment **no longer claims this is a FreeBSD/ZFS workaround**, retiring the Phase 33 framing in the code and not merely in the register. Seven now-dead includes were removed with it, one carrying a comment that had become false. **The platform layer (D3) is live:** `hikari_platform_probe()`/`_log()` emit one `wlr_log(WLR_INFO)` block early in `server_init()`, recording `render_buffer_caps` (the probe W3's blur backend will branch on), the renderer's DRM node resolved by device-number match against `/dev/dri`, the `card*` count, and a **live** `posix_fallocate()` probe on `XDG_RUNTIME_DIR` — and when more than one GPU is present it prints the `WLR_DRM_DEVICES` override right next to the symptom, putting the FB-3 fix in the log. **FB-8 is fixed and verified:** `.ifdef` tests definedness rather than value, so until now *no* feature could be turned off from the command line; all 11 switches converted and checked across the matrix with the default configuration unchanged. A tidier `.for`+`.undef` form was tried and **rejected on evidence** — `.undef` cannot remove a command-line variable in bmake and would have silently not worked. Validation: **0 warnings across all 60 files in both build configurations**. Not built, not linked.
- **Two decisions tabled (see TODOS Phase 72):** **(1) FB-6** — retire the never-set, permanently-broken `WITH_POSIX_C_SOURCE` (recommended), or keep it and add three `__BSD_VISIBLE`-guarded declarations across three files. Held rather than guessed, because one option removes a feature (AGENTS.md §3) and the other spreads a workaround over three files (standing anti-debt directive). **(2) libdrm** — adding it would let the startup log name the driver (`i915` vs `nvidia-drm`) instead of an inferred device path, which is strictly better FB-3 evidence; MIT-licensed and already header-reachable, but a real new link dependency and outside approved scope.
- **Phase:** Phase 71 (**W5 + W6 IMPLEMENTED, awaiting the user build**). First code workstream of the Phase 70 plan; 76 insertions / 3 deletions across `src/server.c`, `src/lock_mode.c`, `hikari_unlocker.c`. **F3 was two bugs, not one:** besides the site the plan named, `disable_outputs()` (`lock_mode.c:507`) dereferences the same timer pointer unguarded and is reached **directly** by `key_handler`'s Ctrl+C branch — so a failed allocation faulted on a keystroke, not merely at lock time. Both guarded; a lost timer degrades to "never blanks" with a `wlr_log(WLR_ERROR)`, which Q2's `0 = never` makes a legitimate state anyway. **C1 restores X11↔Wayland clipboard** — `wlr_xwayland_set_seat()` was called nowhere in the tree (and nowhere upstream either), so copy/paste has never crossed the boundary in either direction; it now runs after `setup_selection()`, the only point where the seat exists. **C2** adds `ext-data-control-v1` beside the `wlr-` variant; **C3** guards both manager returns non-fatally. **Two of the plan's own instructions were found wrong during implementation and are corrected in DECISIONS_LOG Phase 71:** the "add a `seat_destroy` guard" step (wlroots owns that listener privately — adding one would be duplicate state), and F5's stated justification (nothing was "silently consumed"; `locker_result_handler` already recovered via the hangup, so the real gain is an immediate deny rather than one waiting on process teardown). **F4 deliberately held** — it is conditional on W0-6, which has not been run. Validation: 0 warnings on all three files under the Phase-68-corrected clang invocation, including `server.c` built **without** feature macros to exercise the `#ifdef HAVE_XWAYLAND` guard as false, plus `nm -D` confirmation that all three newly-called symbols are exported. Not built, not run.
- **Phase:** Phase 70 (**Lock-screen investigation complete, Option B plan approved, NO CODE CHANGED — no step yet approved for execution**). The requested feature was found not to exist: `grep -ri blur` returns one hit tree-wide (a HiDPI comment at `bar.c:679`), nothing appears in any tracker or in git history, and branch `lockscreen` is identical to `master`. It is greenfield, not a regression. What the investigation *did* find is that **the lock screen hides nothing (F1, CRITICAL)**: `override_visibility()` (`lock_mode.c:749`) flips flag bits only, and the two sites that carry that flag onto the scene graph (`view.c:1157`/`:1193`) both `assert(!is_forced)` — precisely the state lock mode establishes, so it structurally cannot use them. Upstream worked because it composited the lock screen in its own renderer (`git 7777aaa:renderer.c:823`: dimmed background + public views + indicator); the wlroots-0.20 port deleted `renderer.c` and **never wrote a scene-graph equivalent**. Private window contents, the top bar and every layer surface are therefore on screen for the ~1 s before the blank and for a fresh 10 s after each password keystroke. Alongside it: **F2** (a window mapping while locked renders, and the code comment claims the opposite), **F3/F4/F5**, and **C1 — `wlr_xwayland_set_seat()` is called nowhere, so X11↔Wayland clipboard has never worked** (verified absent upstream too, so not a port regression). The lock *security boundary itself* — keyboard routing, cursor deactivation, switch gating, `mlock`/`explicit_bzero`, absolute helper path with `closefrom` — was traced end to end and is **sound**; every defect is in rendering. **Two corrections to the record:** a new **§5 H0** hypothesis (this is a hybrid Intel+NVIDIA laptop with `hw.nvidiadrm.modeset=1`, never considered by Phase 19, and it explains *both* eDP-1 log lines with one cause — the `EGL_EXT_device_drm` failure is NVIDIA-EGL behaviour, not broken Mesa), and the retirement of **Phase 33's "GBM mapping fails on FreeBSD" framing** (wlroots 0.20.2 exposes no public shm/CPU allocator at all, so the custom `wlr_buffer_impl` is idiomatic on every platform; the real debt is that it exists twice). Option B (live-screen blur) is confirmed implementable entirely on public 0.20.2 API with **no custom shaders**. Full detail: DECISIONS_LOG Phase 70; plan in PLANS item -12; new BLUEPRINT §13 FreeBSD register.
- **User rulings this phase (Q1–Q4):** hold for the proper W2 fix rather than an interim F1 patch; blank timeout defaults **180 s on AC / 60 s on battery, all configurable**; blur backend **CPU baseline first, GPU second**; persist the plan to `.devdocs/` (done).
- **Phase:** Phase 69 (**review round 4 -- two findings against Phase 68's own code, both valid, both FIXED, awaiting the user build**). **(1) Both DISPLAY `setenv()` calls ignored their return value.** At `setup_xwayland()` that is silently dangerous: a failed `setenv` leaves DISPLAY untouched, which either reinstates the Phase 68 lazy-start deadlock verbatim (launched via the wrapper, which unsets DISPLAY) or -- worse -- leaves DISPLAY still naming the user's separate `Xorg :2`, so every X client `run_autostart()` spawns connects to a foreign display and appears to vanish. Now fatal, with a diagnostic. The same check was added to `xwayland_ready_handler()` but **deliberately kept non-fatal**, departing from the finding: post-Phase-68 that call is a redundant re-export running from a live event handler with a valid DISPLAY already in place, and killing a running session over it would destroy more than it protects. **(2) Phase 68's own logging pipeline was masking the compositor's exit status.** `exec ... | tee -a` reports the *last* pipeline command's status, so a compositor killed by SIGSEGV surfaced as a clean **exit 0** -- verified empirically against the replacement, which correctly reports **139**. `exec` before a pipeline also replaces only a subshell, so hikari was not the top-level process. Fixed by redirecting the wrapper's own descriptors (`exec >> "$HIKARI_LOG" 2>&1`) before a now-genuine exec, which also collapsed the duplicated dbus branches; `pipefail` was unavailable under `#!/bin/sh`. **This was the most consequential item of either review round** -- left in place it would have defeated the exact debug cycle Phase 68 A exists to enable. Full detail: DECISIONS_LOG Phase 69.
- **Phase:** Phase 68 (**diagnostics + XWayland deadlock + NULL-deref class + clang-format -- IMPLEMENTED, awaiting the user build**). Four workstreams batched into one build cycle. **(A) The launcher never captured stderr.** `wlr_log` writes only there, so every diagnostic was discarded -- Phase 53 identified this and it was never fixed, and it is the direct reason three crash investigations ran blind. Added an opt-in `HIKARI_LOG` tee. The rest of the diagnostic surface turned out to be **healthier than the docs claim**: `/var/coredumps` exists, `kern.corefile` is set, `ulimit -c` is unlimited, three hikari cores are on disk, and both gdb and lldb are installed -- **correcting Phase 53's record that the directory did not exist**. **(B) The Phase 65 P0 is root-caused.** `setup_xwayland()` requests lazy mode, and `DISPLAY` was exported only from the `ready` handler. wlroots opens the X socket and fills `display_name` inside `wlr_xwayland_create()`, but in lazy mode it does not exec Xwayland until a client connects -- and no client can connect without `DISPLAY`, so `ready` never fires and `DISPLAY` is never set. A closed loop in which XWayland could **never** start; `start-hikari.sh` also `unset DISPLAY` beforehand, removing any inherited fallback. Fixed by exporting `display_name` right after the NULL guard, which is exactly what the installed 0.20.2 header prescribes. This explains all three Phase 65 symptoms and **supersedes the Phase 64 XWayland render-gap finding, which has been untestable all along**. **(C) The Phase 67 NULL-deref class was 7 sites, not 3.** All 64 `wlr_*_create*` calls were enumerated and each hit hand-verified; two further defects surfaced -- `idle_notifier`, which is only dereferenced once a media client takes an idle inhibitor (so a NULL faults minutes in, with no apparent link to init), and `server->seat`, whose `assert()` guard is dead code under `-DNDEBUG` and therefore read as guarded while being unguarded. **(D)** `.clang-format` never loaded because `Language: C` is not a valid value in any release; fixed to `Cpp`, style otherwise untouched per user instruction. Full detail: DECISIONS_LOG Phase 68.
- **The validation procedure of Phases 61-67 was not validating anything.** The recorded `cc -fsyntax-only -Wall` used the wrong compiler -- `cc` is Red Hat GCC 11.5 in the agent sandbox, while the platform compiler is FreeBSD clang 19.1.7 -- and defined none of the five feature macros, so every `#ifdef` region went unchecked, including `setup_xwayland` and both regions fixed in Phase 67. Under clang with all macros and `pkg-config` flags the tree is **0 warnings across 60 files**. Two consequences: the "cosmetic enum-compare warnings" backlog item is **stale**, and **`DEBUG=YES` (which adds `-Werror`) will compile** -- previously unknown, and the fact that unblocks the debug plan. Command recorded in DECISIONS_LOG Phase 68.
- **234 `assert()` calls across 32 files are dead code** (`view.c` alone: 101). `-DNDEBUG` in release; `strings hikari` returns 0 assert strings. Phase 61 approved the replacement policy; only a handful of sites have had it applied. Scoped as its own project (TODOS P2).
- **Phase:** Phase 67 (**External review round 3 — two findings, both valid, both FIXED, awaiting user build**). Two `src/server.c` defects, each verified against current code before editing; nothing was rejected this round. **(1) Layer-shell NULL dereference.** `setup_layer_shell()` handed `wlr_layer_shell_v1_create()`'s result straight to `wl_signal_add`, which takes the address of a member — so on allocation failure it computed an offset from `NULL` and wrote through it, segfaulting at startup instead of failing cleanly. Guarded with the `pointer_gestures` fatal-exit pattern that already sits six lines from the call site. The graceful-degradation alternative was put to the user and declined: it needs an extra `wl_list_init` purely to stop `hikari_server_stop()`'s unconditional `wl_list_remove` from converting a startup crash into a shutdown one — the same "link left as `hikari_malloc` garbage" class Phase 56 found in `hikari_view_init()`. **(2) Virtual pointer hijacked the entire cursor.** `new_virtual_pointer_handler()` called `wlr_cursor_map_to_output()`, which maps the *whole cursor*, not one device — so any client binding `zwlr_virtual_pointer_v1` with a suggested output permanently confined the user's **physical** mouse, touchpad and touchscreen to that output, unrecoverable short of restarting the compositor. Replaced with the per-device `wlr_cursor_map_input_to_output()`; this is the pointer-side counterpart to **Phase 50 Finding 2**, which fixed the same class on the touch side. Full detail: DECISIONS_LOG Phase 67.
- **Validation procedure corrected for Phases 61-67.** These phases record their validation as `cc -fsyntax-only -Wall`. That command does not run in this environment at all (`/usr/local/include/libdrm/drm.h` needs `__kernel_size_t`), and even given the right headers it defines neither `HAVE_LAYERSHELL` nor `HAVE_VIRTUAL_INPUT` — so **every `#ifdef`-guarded region was silently skipped, including both regions fixed in Phase 67**. With a two-line `__kernel_size_t` shim, `pkg-config --cflags`, and both macros defined, `src/server.c` checks clean. The working invocation is recorded in DECISIONS_LOG Phase 67 and should replace the bare command going forward. `make` remains unavailable (`server.o`, `hikari` are `root:wheel`); a clean syntax check is not proof it links.
- **`clang-format` is blocked, not merely pending (TC-FORMAT-01).** The installed `clang-format` rejects this repo's `.clang-format` with `unknown enumerated scalar` on `Language: C` — a version mismatch. The compliance run cannot be done here until the version is pinned or the config relaxed.
- **Phase:** Phase 66 (**License and Branding Update — IMPLEMENTED**). Updated the `LICENSE` file to a comprehensive 2-Clause BSD license under "Orpheus497" (2026), while retaining the original upstream attribution from "raichoo". Swept `README.md` to ensure all prose and documentation branding refers correctly to "Hikari Sakura" while strictly preserving binary names (`hikari`, `hikari-unlocker`) and paths per Phase 51 rules.
- **Phase:** Phase 64 (**cursor offset fixed, review triaged, doc standard cleaned — awaiting user build**). The cursor bug was a coordinate-space error, not a cursor error: `surface_at()` in `src/xdg_view.c` passed **window-geometry-local** coordinates to `wlr_xdg_surface_surface_at()`, which takes **wl_surface-local** ones. They differ by `xdg_surface->geometry.x/y`, the CSD margin, non-zero for essentially every GTK client — so every hit test landed that far up and to the left of the real pointer. Rendering was never wrong (`wlr_scene_xdg_surface_create()` applies the same correction with the opposite sign), which is exactly what disguised it as a cursor problem. The same mismatch in the damage path was checked and deliberately left alone: both damage helpers discard the rectangle and only schedule a frame, because the scene graph does the real damage tracking. **External review:** 15 findings verified individually against current code — **7 implemented, 5 rejected as invalid** with the verification disproving each, **2 declined as policy**. The most consequential was premultiplied alpha: wlroots requires it for scene rectangles while hikari stores straight RGBA (correct for Cairo), so any `"#RRGGBBAA"` border, indicator or background rendered too bright — **this corrects Phase 60's record** that those files were "already alpha-correct". Also normalised 17 `##`-prefixed comments matching no `AGENTS.md` prefix. Full detail: DECISIONS_LOG Phase 64.
- **CONFIRMED AS FACT (Phase 70), was "NEW, awaiting approval" — XWayland views render no content.** `src/xwayland_view.c` builds a `scene_tree` (`:537`) and attaches **only** border and indicator_frame; no `wlr_scene_surface_create`/`wlr_scene_subsurface_tree_create` exists in the file. Phase 70 additionally established that **`src/xwayland_unmanaged_view.c` contains no `wlr_scene` reference whatsoever**, so override-redirect surfaces (menus, tooltips, dropdowns, drag icons) are never attached either. Scheduled as Phase 70 **W8**, which **must not land before W2** — XWayland content being invisible is currently the only thing limiting F1's exposure to native Wayland clients, so fixing this widens the security hole. **This corrects Phase 62's reasoning** — it attributed Firefox surviving the popup abort to being an XWayland client, but if XWayland content does not render at all, that Firefox was native Wayland and simply had no menu open. The Phase 62 fix itself stands; it was proven by core dump. Confirm with `xterm` or `xeyes`.
- **Build status:** nothing across Phases 61-64 has been compiled under the real Makefile. Every touched file passes `cc -fsyntax-only -Wall`; the agent cannot run `make` because the build artefacts are root-owned.

- **Phase:** Phase 63 (**popup scene nodes + shutdown NULL deref FIXED — awaiting user build**). The session ran **11 minutes with no runtime crash** — a first for this project — with VT switching, Firefox and pavucontrol all holding. Two defects surfaced. **(1) xdg popups have never rendered.** `xdg_popup_create()` never created a scene node, on the strength of a comment claiming `wlr_scene_xdg_surface_create()` "already manages popup scene nodes automatically". It does not: that helper calls `wlr_scene_subsurface_tree_create()`, which walks **subsurfaces**, and nothing in `types/scene/xdg_shell.c` traverses **popups** — tinywl calls the helper once per popup for exactly this reason. So every right-click menu, submenu and combo-box dropdown was invisible. It stayed hidden as a bug only because until Phase 62 *creating* a popup aborted the compositor first. `src/layer_shell.c` had the identical gap, which **retroactively explains the long-standing "Layer-client spot check (waybar with sub-menus)" backlog item**. Fixed in both files by adding a `scene_tree` parented to the parent surface's tree, with xdg popups publishing their tree on `base->data` so nested submenus resolve correctly; wlroots owns the tree lifetime so the destroy paths are unchanged. **(2) Shutdown SIGSEGV** (third core dump, `hikari.4177.1001.core`): `hikari_workspace_focus_view()` dereferenced `hikari_server.workspace` unguarded, but `hikari_output_fini()` NULLs it while tearing down the noop output, and at shutdown a real output can be finalised after that. Fixed with the approved safe-bail pattern. Full trace: DECISIONS_LOG Phase 63.
- **Cursor offset — diagnosed, not yet fixed.** `xdg_view.c`'s `surface_at()` hands window-geometry-local coordinates to `wlr_xdg_surface_surface_at()`, which is documented as taking **surface-local** ones; the difference is `xdg_surface->geometry.x/y`, the CSD shadow margin, non-zero for essentially every GTK client. Rendering is unaffected because the scene graph applies the offset itself, so the pointer draws correctly while hit-testing lands offset by the shadow width. Needs the same check applied to `xwayland_view.c` and `layer_shell.c` before changing anything.

- **Phase:** Phase 62 (**SECOND ROOT CAUSE FOUND AND FIXED — awaiting user build**). With Phase 61's NULL-deref fix installed, the user confirmed VT switching survives and Firefox is fine — but **pavucontrol crashed the compositor immediately**, producing a second core dump (`hikari.52741.1001.core`, signal 6). This is a completely separate defect from Phase 61. `xdg_popup_create()` called `popup_unconstrain()` at popup-creation time; `wlr_xdg_popup_unconstrain_from_box()` ends with `wlr_xdg_surface_schedule_configure()` (`wlr_xdg_popup.c:534`), which asserts `surface->initialized` (`wlr_xdg_surface.c:168`) — and wlroots emits `new_popup` straight from the client's `get_popup` request, **before the popup surface has ever been committed**. So hikari aborted on **every xdg_popup ever created**: every GTK menu, combo box, dropdown and tooltip. Deterministic, not a race. **This retires the "lots of children/background processes" theory entirely** — the real predictor is *native-Wayland clients that open popups*. Firefox under XWayland never creates an xdg_popup (survives); pavucontrol opens one on launch (dies instantly). Fixed in both `src/xdg_view.c` and `src/layer_shell.c` by deferring the unconstrain into the popup's existing `initial_commit` branch. Notably, **hikari already understood this exact wlroots 0.20 constraint for toplevels** — `hikari_xdg_view_init` carries a comment citing `wlr_xdg_surface.c` line 168 as the reason `wlr_xdg_surface_ping` was removed — but never applied it to popups, in either file. Full trace: DECISIONS_LOG Phase 62.

- **Phase:** Phase 61 (**CRASH ROOT-CAUSED AND FIXED — awaiting user build**). The compositor crash is a **NULL pointer dereference in hikari's own `session_active_handler`**, proven by the first core dump ever captured in this project. `session_active_handler()` read `*(bool *)data`, but wlroots emits `session->events.active` with `data == NULL` (`backend/session/session.c:27`, `:33`). That is an unconditional segfault on **every** VT switch and every seat disable — 100% deterministic, not a race and not memory corruption. `gdb bt` on `/var/coredumps/hikari.27920.1001.core` puts frame #0 at `session_active_handler+10`, the first memory access after the prologue, reached libseat -> wlroots -> `wl_signal_emit_mutable`. Fixed by reading `server->session->active` instead. **Two prior conclusions are corrected:** (1) there were always *two* crash signatures — `/var/log/messages` shows SIGSEGV (11) at 13:59:15, 14:26:15 and 14:51:15 alongside the SIGABRTs, so the "SIGABRT, not SIGSEGV" premise that drove Phases 53-57 was half wrong; (2) the captured crash printed **no assertion message** and exited 139, so Phase 57's prediction of a wlroots assertion did not hold for this fault. Why it looked app-triggered: the user runs hikari from a TTY while `Xorg :2` holds VT `v1`, so anything pulling the VT back disabled hikari's seat and killed it ~15s in. Full trace: DECISIONS_LOG Phase 61.
- **Phase 61, Finding A** (**the incomplete refactor, FIXED**). `hikari_xwayland_unmanaged_evacuate()` updated `->workspace` but never moved `unmanaged_output_views` to the destination output — unlike its managed twin `hikari_view_evacuate()` (`view.c:1610-1619`), whose comment names the exact hazard. This sits on the *same* code path as the segfault: wlroots' `handle_session_active` destroys every connected output on session-deactivate, so `hikari_output_fini()` had already run ~66ms earlier. Every live X11 menu/tooltip/dropdown was left holding a link into a freed `wl_list` head — the most probable source of the SIGABRT half of the crash log. Fixed, plus: link initialised at init, remove-then-init convention, idempotent `unmap()`, new `hikari_xwayland_unmanaged_detach()`, a last-resort sweep in `hikari_output_fini()`, and NULL-workspace safe-bails.
- **Phase 61, Finding B** (**FIXED**). `override_redirect` was read once at new-surface time and never revisited, so GTK/Chromium windows that flip the attribute mid-life — menus, tooltips, dropdowns — stayed in the wrong view type permanently. Added `hikari_server_adopt_xwayland_surface()` as the single adoption point, `set_override_redirect` listeners on both view types, and already-mapped adoption in both `_init`s.
- **Assertion policy decision (closes Phase 54 W3's open question):** `strings hikari` → **0** assert strings (release `-DNDEBUG`); `strings libwlroots-0.20.so` → **280**. Every `assert()` written into hikari over the last 50 phases is dead code in the shipped binary, including everything Phases 55/56/57 added as a safety net. User chose **always-on `wlr_log(WLR_ERROR)` + safe bail** over debug-only. The safe-bail guards in this phase are the first instances; Step 3 generalises it.

- **Phase:** Phase 60 (**Top bar centre lane + alpha-capable colours — IMPLEMENTED, awaiting user build**). Phase 58's two cosmetic issues are now both implemented: the indicator overlay is gated on the Logo key (Phase 59), and the top bar has a real centre lane with the clock/date centred, network/brightness/volume/battery right-aligned in that reading order, and a translucent background. Alpha is expressed as a quoted `"#RRGGBB"`/`"#RRGGBBAA"` string — **not** as an integer, because `0x0080FFCC` and `0x80FFCC` are indistinguishable once UCL parses them, so a digit-count heuristic would silently misread any colour whose red channel is zero. Integers keep their existing opaque meaning, so no existing config changes appearance. A dedicated `bar` colour was added beyond the requested option 3, because the bar previously painted from `clear` (the desktop background) and fading it would otherwise have faded the desktop behind every window. Nine duplicated colour-parse blocks collapsed into one shared `parse_color()`. Full detail: DECISIONS_LOG Phase 60.
- **Phase 57** (**ROOT CAUSE FOUND AND FIXED — awaiting user build**). The window-close crash is a wlroots-side assertion, not a hikari memory bug. `src/xdg_view.c` registered `request_fullscreen` on the **xdg_toplevel** but removed it in `destroy_handler`, which is bound to the **xdg_surface**. wlroots 0.20 destroys the toplevel role object *before* emitting the xdg_surface destroy signal, and `destroy_xdg_toplevel()` asserts every toplevel signal has no listeners left — so wlroots `abort()`ed (SIGABRT) on every window close, three lines before hikari's cleanup ran. Fixed by adding a `toplevel_destroy` listener on `xdg_toplevel->events.destroy` that releases the toplevel-scoped listener (and itself) at the right moment. **Two corrections to earlier phases:** (1) Phase 53 concluded the binary was a `DEBUG=YES` build with hikari assertions live — wrong; it is a release `-DNDEBUG` build with hikari's assertions compiled out, and the aborting assertion was inside `libwlroots-0.20.so`, which *is* built with assertions on. (2) The Phase 56 visibility refactor **was present in the binary that crashed at 13:46:57** — it fixed a real separate latent defect class but was never the cause of this crash. Full trace: DECISIONS_LOG Phase 57.
- **Phase 56** (Single-Writer Visibility Transitions — **IMPLEMENTED, NOT YET BUILT**). Steps 0-2 of the Phase 55 refactor are applied to `src/view.c` and `src/group.c`. Visibility linkage now has exactly two writers (`view_link_visible_at()` / `view_unlink_visible()`) instead of being spread across `increase_group_visiblity()` + `place_visibly_above()` + `hide()` + an inline copy in `hikari_view_lower()`, all now deleted. **The root-cause fix is in `hikari_view_unmap()`:** the branch that set the hidden flag *without* performing the unlink — leaving a view linked into three lists while `detach_from_group()` freed its group — is gone; `view_unlink_visible()` sets the flag itself, so flag and linkage can no longer diverge. Two extra latent holes were closed during implementation (`hikari_group_init()` never initialised `visible_server_groups`; `hikari_view_init()` initialised 1 of 7 links). One self-inflicted regression was caught mid-refactor by the IDE's live diagnostics and resolved by splitting the unlink into group-scoped and full-transition variants — see DECISIONS_LOG Phase 56. **NEXT ACTION IS THE USER'S: `sudo make clean && sudo make install`, then test closing a window / clicking a popup button. Nothing has been compiled or run** (IDE-tooling-only directive). Still outstanding: the deferred consistency checker (item 1c), `BLUEPRINT.md` docs (Step 3), and the headless smoke test (Step 4).
- **Phase 55** (Root-Cause Architecture Analysis + Single-Writer Visibility Refactor) — analysis and plan; superseded by Phase 56's execution. Answers the question "is the architecture itself causing the crashes": **yes, and the flaw is nameable** — the fact *"this view is visible"* is stored in **six** independent representations (hidden flag, `workspace->views`, `hikari_server.visible_views`, `group->visible_views`, the `group->visible_server_groups` aggregate, and the scene-node enabled bit) with **no single writer**. Entry is split across two functions callers must remember to pair; exit is one function; `hikari_view_lower()` hand-writes the whole linkage a third time. Group lifetime then depends on an unwritten, unasserted invariant linking two of those representations — and `hikari_view_unmap()` contains a branch that sets the hidden *flag* without performing the *transition*, then frees the group, producing a simultaneous three-list + freed-group use-after-free. That is exactly the delayed-corruption signature behind the long history of "random" crashes and the current SIGABRT-with-no-core. Remediation is a bounded refactor — **Single-Writer Visibility Transitions** — confined mostly to `src/view.c`, ~18 call sites, fully wired function-by-function in `PLANS.md` item -6. Explicitly **not** the Phase 44 DOD/SoA rewrite (that rejection stands; allocation strategy is unchanged). Honest caveat: this removes a defect *class* plus one specific identified UAF path, but has not been proven to be the exact live crash, because no crash output or core has been captured yet — Step 4 (headless smoke test under `MALLOC_CONF=junk:true`) closes that gap and should be built first.
- **Phase 54** (View-Teardown Ownership-Graph Hardening) — **PLAN WRITTEN, AWAITING APPROVAL, no code changes made.** Addresses the structural fragility behind the recurring crash-fix cycle: view teardown hand-sequences 7 `wl_list` links and 6 owning pointers, across 65 call sites and 5 entry points, with no mechanical verification. Plan is four workstreams (W1 document the graph → W2 close a found latent defect → W3 lifecycle state + invariant checker → W4 headless smoke test under jemalloc junk-fill). **A live latent defect was found while analysing:** `hikari_view_init()` initialises only 1 of the 7 list links — four hold `hikari_malloc` garbage until map, safe today only via an unwritten, unchecked invariant guarding a `wl_list_remove()` through garbage pointers. Three questions need user decisions before implementation (scope, W3 release-build policy, W4 priority) — see `PLANS.md` item -5. Note W4 is also the fastest route to resolving the still-open Phase 53 crash.
- **Phase 53** (Close-Window / Popup-Button Crash) — **INVESTIGATED, NOT YET RESOLVED — empirical reproduction required next.** User reports the compositor crashes unconditionally when closing a window or clicking a button in a popup. A full re-audit of the Phase 42/44/45 popup/subsurface teardown fix (traced against actual wlroots 0.20 signal ordering, not assumption) found it structurally sound — that specific, previously-diagnosed bug is not the cause of this crash. Live forensics on the FreeBSD target (`ps`, `dmesg`, `/var/log/messages`, binary inspection) instead found the real signature: **the compositor is aborting (SIGABRT / signal 6), not segfaulting** — 4 times today, 3 of them after the current fully-patched binary (`da582a7`) was installed, confirmed byte-identical between the repo build and `/usr/local/bin/hikari`. The installed binary was built with `DEBUG=YES` (has `-g`, not stripped), so every `assert()` in the codebase is live, not compiled out. No crash output was captured (`start-hikari.sh` doesn't redirect stderr) and no core dump exists (`/var/coredumps/` — the configured `kern.corefile` target directory — doesn't exist on this system, so every dump silently failed). Full writeup: `DECISIONS_LOG.md` Phase 53. **Next step is empirical, not more static reading**: reproduce once with stderr captured to a file (and ideally with `/var/coredumps/` created first) to get the exact assert/abort message — this resolves the 3-way ambiguity (hikari assert violation vs. hikari's own OOM fail-fast abort vs. jemalloc heap-corruption detection from an unrelated, still-undiscovered UAF) in one line instead of more guessing.
- **Phase 52** (Post-Install Config Load Failure) — RESOLVED. Root cause: a bare-modifier keybinding (`"L" = action-menu`, no key) in the user's `~/.config/hikari/hikari.conf:160`, silently rejected by `hikari_binding_config_key_parse()` with zero diagnostic anywhere in the call chain — the only message that ever printed was `server.c`'s generic load-failure wrapper, which is why it looked unexplained. User fixed their config; the matching hikari-side silent-failure bug was fixed in `src/binding_config.c` (added the missing `fprintf`). Confirmed unrelated to Phase 50's touch/gesture work — the user's `gestures {}` block parses cleanly, and config load completes before any touch/gesture code can run. Full trace in `DECISIONS_LOG.md` Phase 52. Two low-severity, unapproved follow-ups noted but not actioned: the identical silent-failure gap in the mouse-binding parser, and a redundant duplicate `wl_list_init(&server->outputs)` in `server_init()`.
- **Phase 51** (Documentation Rebranding & FreeBSD Overhaul, concurrent session) complete. **Phase 50** (Touch/Gesture Correctness & Completion) steps 1-6 implemented (uncommitted); step 7 (user-run build + runtime verification) is now unblocked since the compositor can actually start again. See `PLANS.md` item -2 and `DECISIONS_LOG.md` for the full writeup. Summary: fixed the CRITICAL touch coordinate-space bug plus a hard compile error found live in `touch_cancel`; added per-device output mapping; implemented a full `inputs { gestures {} }` config-binding system with buffer-and-replay dispatch; implemented touch-as-click window management; documented all of it in `hikari.conf`/`hikari.md`/`README.md`/`BLUEPRINT.md` on top of the Phase 51 rebrand.
- **Branch:** wlroots-0.17.1 (PR source ref; the dependency itself is wlroots 0.20.2 — see Makefile:148-149, confirmed via `pkg-config --modversion wlroots-0.20`)
- **Overall progress:** Documentation rebranded to Hikari Sakura (Phase 51) and now also documents touch/gesture support (Phase 50 Finding 5, closed). Windows render and the Phase 40 NULL-output guards are in place. Phase 42's audit found a concrete root cause for crashes (resolved in Phase 45-47).
- **Target OS:** FreeBSD 15.1-RELEASE (ZFS root)
- **Current step:** Awaiting a user build (`sudo make clean && sudo make install`) and runtime test of Phase 50's touch/gesture changes (`PLANS.md` item -2 step 7 / `TODOS.md` P5).
- **Blockers:** eDP-1 scanout swapchain test failure — Mesa/EGL/GBM ↔ drm-kmod layer (pre-existing, below hikari), unrelated to Phase 42/50. The root-owned build-artifact blocker from prior phases is resolved.

## Remaining Work

*Refreshed 2026-08-22 14:46 (Phase 81). **W0–W8 are complete except W7b.** The lock-screen programme, the clipboard work, the scene-graph restructure and XWayland integration are all delivered and hardware-confirmed.*

### Decisions the user needs to make
- **Approve W7b (`ext-foreign-toplevel-list-v1`)?** The one workstream not delivered. Serves taskbars (waybar `wlr/taskbar`) and future window-selection portals; *not* required for screen sharing. Six touch points in `src/view.c`, so it wants its own build cycle.
- **Approve the deferred `forced`-flag removal?** Pure cleanup — F1/F2 are already fixed by the layer trees. 15 sites, several provably unreachable, in the file behind eight crash phases.
- **Document `ui { lock { ... } }` in the man page?** `etc/hikari/hikari.conf` documents it fully; `share/man/man1/hikari.md` does not.
- **libdrm as an explicit dependency?** Would let the startup log name the driver (`i915` vs `nvidia-drm`) instead of an inferred device path — better FB-3 evidence. MIT, headers already reachable, but a real new link dependency.
- **Configurable lock-screen clock offset?** Each visual tweak currently costs a rebuild and re-login.

### User-run, still outstanding — highest value first
- **W0 diagnostic matrix** (7 read-only commands, `TODOS.md` Phase 70). **W0-1 (`WLR_DRM_DEVICES=/dev/dri/card0`) is now the single highest-value command available:** it tests FB-3, which may close *both* the eDP-1 blocker open since Phase 19 **and** the residual OBS black-frame problem, since both are hybrid-GPU dmabuf in origin. **W0-6** gates F4/P2-14.
- **PAM unlocker live verification** (setuid 4555 path).
- Phase 50 touch/gesture runtime checks; Phase 40 multi-window guards.

### Known-open defects
- **OBS ScreenCast black** — compositor side verified working; failure is downstream in portal-wlr → PipeWire → OBS. See Phase 81.
- **F4 / P2-14** — `hikari_output_enable()` re-enables without setting a mode. Conditional on W0-6; may need no fix at all.
- **FB-3 / FB-4** — hybrid Intel+NVIDIA device selection and the eDP-1 scanout swapchain failure. The only genuinely open platform defects in BLUEPRINT §13, and now the prime suspect for the OBS problem too.

### Delivered and confirmed on hardware this session
Lock screen with live blur + compositor-drawn clock (W3/W4) · scene layer trees, F1 + F2 fixed (W2) · platform probe and buffer consolidation (W1) · lock-mode and unlocker fixes (W5) · clipboard incl. X11 bridge (W6) · portal + capture protocols (W7a) · **XWayland renders content for the first time** (W8).

*Granular tasks are tracked in `.devdocs/TODOS.md`.*
